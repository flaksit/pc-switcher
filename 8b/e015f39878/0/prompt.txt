Is name test_core_us_job_arch_as1_job_integration_via_interface() good? Not better something that reflects the purpose of general, all-comprising e2e test?

---

We interleaved folder_sync into test_core_us_job_arch_as1_job_integration_via_interface(). However, now we are using area_ marks so we only run specific tests based on which files changed + the smoke tests, we maybe shouldn't have the folder_sync test in this complex test that checks some complex scenarios of the core logic.
Maybe we'd better have a separate "general all-comprising e2e test" (in smoke and each area it covers; which should be all) that does a single first sync (or maybe a sync + back-sync), including folder-sync (and we will integrate a package-sync as well, later). And leave the complicated scenario specific to core in its own test in area_core.
What do you think?

---

Nice plan. For 3 ("dry run"), we need to be sure that "writes nothing" is tested for each and every part of pc-switcher. Probably most economic way: in a single e2e test like the global smoke e2e. But needs not be in smoke; needs to be in each area it covers.

---

Nice plan. For 3 ("dry run"), we need to be sure that "writes nothing" is tested for each and every part of pc-switcher. Probably most economic way: in a single e2e test like the global smoke e2e. But needs not be in smoke; needs to be in each area it covers.

Ok? Keep package-sync out of scope of this plan.
If plan ok with that, delegate to subagents.

---

<task-notification>
<task-id>a2a877ea63e5a5041</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-216/1c04a89f-42a4-4165-82a3-dd281279ab6c/tasks/a2a877ea63e5a5041.output</output-file>
<status>completed</status>
<summary>Agent "Slim smoke e2e, move gate steps" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Done. Only my two files are modified; nothing committed.

Smoke test (`test_end_to_end_sync.py`, still `smoke` + `area_folder`), now 2 syncs:
- Renamed `test_core_us_job_arch_as1_job_integration_via_interface` → `test_sync_runs_every_job_and_replicates_home_both_directions`; requirement IDs (CORE-US-JOB-ARCH-AS1, ADR-015/016) live in the docstring only.
- Kept: A→B `--allow-first-sync` (with `_assert_job_integration`, manifests, metadata details, exclusions, included markers, filter outcomes) and the mutate-pc2 → B→A propagation check. Sub-step labels renumbered 3a-3f → 1a-1f; docstring rewritten for the two-sync scenario; rationale comments preserved.
- Deleted: the `--dry-run` rehearsal (step 2) outright, the W1-blocked run (step 1), and the second clean A→B (step 5). `assert_tree_absent` and `folder_sync_scenario.STATE_DIR` are no longer referenced from this file; helpers untouched.

Gates module (`test_sync_order_gates.py`, `area_core`):
- Already covered, so deleted rather than moved: the clean-round-trip claim (ADR-015 #159) is proven by `TestConsecutiveSyncWarning::test_back_sync_clears_warning` step 3 — pc1→pc2, pc2→pc1, then pc1→pc2 with no flags must succeed. Nothing added for it.
- Genuinely missing, so added: W1 blocks non-interactively. Folded in as a new step 1 of `test_consecutive_sync_warning_workflow` (no new test, no new fixture cost) — that test's step 1 previously always passed the flag, so the block was never exercised. Asserts the run fails, output names `--allow-first-sync` and the abort, and pc2's sync-history is still absent (gate runs before any job, so nothing reached the target). Steps renumbered 2-4; docstring updated.

Sync counts now: smoke test 2; `test_consecutive_sync_warning_workflow` 4 runs, of which 2 abort at the gate before any job (cheap); `test_back_sync_clears_warning` unchanged at 3.

Verified: `ruff format`, `ruff check tests/`, `basedpyright`, `codespell`, `pytest -q` (4653 passed), and `pytest tests/integration --collect-only -q -m ""` (75 collected) all pass. Integration tests were not run (VM fleet/lock).</result>
<usage><subagent_tokens>79286</subagent_tokens><tool_uses>20</tool_uses><duration_ms>194773</duration_ms></usage>
</task-notification>

---

y

---

<task-notification>
<task-id>a9679419416445ff7</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-216/1c04a89f-42a4-4165-82a3-dd281279ab6c/tasks/a9679419416445ff7.output</output-file>
<status>completed</status>
<summary>Agent "Write comprehensive dry-run e2e" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Done. Four checks pass (`ruff format`/`ruff check tests/`, `basedpyright`, `codespell`, `uv run pytest -q` → 4653 passed), and `tests/integration/test_dry_run.py` collects with its markers; `-m area_package` correctly deselects it.

**Files**
- `/home/janfr/dev/pc-216/tests/integration/test_dry_run.py` (new)
- `/home/janfr/dev/pc-216/tests/integration/jobs/folder_sync_scenario.py` (appended `filter_tree_path()` and `assert_manifests_unchanged()` — the only two helpers missing; everything else reused)
- `/home/janfr/dev/pc-216/docs/dev/testing-guide.md` (one line in the module list)

**Markers** (module-level `pytestmark`, each justified in the docstring): `area_folder` (folder_sync is the run's only sync job and its rsync preview must leave the target payload alone), `area_btrfs` (both snapshot steps run and must leave no snapshot), `area_core` (locks, first-sync gate, config sync, sync-history skip). Not `smoke` (a full sync per PR is too costly); not `area_package` (the package rehearsal is already asserted in `jobs/test_package_sync.py` and would cost VM fixtures).

**One run**: `PCSWITCHER_SKIP_INSTALL_ON_TARGET=1 pc-switcher sync pc2 --dry-run`, pc1→pc2, config with a single folder_sync scope = the small seeded filter tree (`pcsw-filter`, seeded via `seed_filter_source`/`seed_filter_target`) — chosen because a real sync would add ~9 files, overwrite one and delete ≥3 there, which is the whole point; the rich ownership matrix would cost more and prove no more. No `--yes`: a rehearsal must finish prompt-free.

**Claims asserted**: exit 0 (the W1 first-sync gate does not block a rehearsal); pc2's tree manifests (mode/uid/gid/path + md5) byte-identical before/after; no entry under `/.snapshots/pc-switcher` on either machine; `sync-history.json` absent on both after; no `~/.config/pc-switcher/config.yaml` on pc2; the whole probed state (history+snapshots+config) equal before/after on both machines; the source log carries the dry-run banner, both lock acquisitions and the gate's "skipping confirmation in dry-run mode"; and folder_sync's `[dry-run] Completed sync of …: N files transferred, …, M deletions` with N&gt;0 and M&gt;0. I verified locally with rsync 3.2.7 that `--dry-run --delete --out-format='%i %n%L'` really emits `*deleting` lines and a non-zero `xfr#`, so both counts are sound.

**Not assertable from an integration test**: install-on-target's no-install rule — proving it needs pc2 to lack pc-switcher or run an older build, i.e. a second mutating fixture; `SKIP_INSTALL_ON_TARGET` is set instead and `area_install` dropped. Also unasserted: ADR-014's "every future SyncJob implements a real preview" (a per-job contract, not observable from one run) and the lock acquisition itself, which is only visible in the log, not in post-run state.

**Documented, not asserted** (legitimate dry-run writes, in comments): the source's log file and both machines' lock files under `~/.local/share/pc-switcher` — the probe deliberately reads only `sync-history.json` there; and `BtrfsSnapshotJob.validate()` may create `/.snapshots` itself, so the snapshot claim is scoped to `/.snapshots/pc-switcher` having no children.</result>
<usage><subagent_tokens>224227</subagent_tokens><tool_uses>49</tool_uses><duration_ms>519805</duration_ms></usage>
</task-notification>

---

install_on_target not in that test? Tested elsewhere?

Are there still other integration tests that cover dry-run?

---

y