---
phase: 01-home-sync-mvp-user-data-sync
plan: 06
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-05
files_modified:
  - tests/integration/test_folder_sync.py
autonomous: true
requirements:
  - REQ-sync-scope-user-data
  - REQ-machine-specific-exclusions
  - REQ-sync-scope-file-metadata
  - REQ-manual-sync-workflow
  - REQ-terminal-ux
user_setup:
  - service: hetzner-test-vms
    why: "The round-trip test runs against the real pc1/pc2 Hetzner VMs (btrfs, sudo, rsync). It cannot pass without them."
    env_vars:
      - name: HCLOUD_TOKEN
        source: "Hetzner Cloud project API token (existing test infra)"
      - name: PC_SWITCHER_TEST_PC1_HOST
        source: "pc1 VM host (existing test infra)"
      - name: PC_SWITCHER_TEST_PC2_HOST
        source: "pc2 VM host (existing test infra)"
      - name: PC_SWITCHER_TEST_USER
        source: "test SSH user on the VMs (existing test infra)"
    dashboard_config:
      - task: "Push the current branch to origin before running — pc1_with_pcswitcher_mod / pc2_with_pcswitcher install pc-switcher from the current git branch."
        location: "git remote (origin)"

must_haves:
  truths:
    - "A VM-isolated test automates the full A→B → mutate-on-B → B→A round-trip and asserts success criteria 1-4 (success criterion 5)."
    - "After A→B, every included file exists on the target and is byte-identical (md5sum) to the source, with owner/group/permissions/POSIX ACLs/mtime preserved (criteria 1 & 2)."
    - "Machine-specific items (.ssh/id_*, .config/tailscale, GPU/fontconfig caches, VS Code cache dirs) are absent on the target, while VS Code User/ state and dev-tool caches are present (criterion 3)."
    - "After mutate-on-B and B→A, the source reflects B's additions, modifications, and deletions byte-identically with metadata preserved, and the same exclusions hold in reverse (criterion 4)."
    - "Independently modifying the target then re-running A→B is blocked by the divergence guard (non-zero exit, divergence message); --allow-divergence proceeds; --dry-run changes nothing and logs a preview (D-06/D-12)."
  artifacts:
    - tests/integration/test_folder_sync.py
  key_links:
    - "Uses existing fixtures pc1_with_pcswitcher_mod, pc2_with_pcswitcher, reset_pcswitcher_state, pc1_executor, pc2_executor; drives real `pc-switcher sync` via SSH."
  prohibitions:
    - "The test MUST NOT configure folder_sync to mirror the real /home or /root (mirror-delete would destroy VM data); it uses a dedicated test directory on a btrfs subvolume."
    - "The test MUST clean up its own test trees and config on both VMs (try/finally), per the testing guide."
---

<objective>
Write the bidirectional round-trip integration test that is the developer-facing milestone success metric (ROADMAP success criterion 5). It automates A→B, mutate-on-B, B→A against the real pc1/pc2 VMs and asserts criteria 1-4: byte-identical content, preserved metadata, honored machine-specific exclusions (both directions), propagated deletions, plus the divergence guard (D-06) blocking and the dry-run preview (D-12) doing nothing.

Purpose: Unit tests prove the logic; this proves the real-world behavior — real rsync-as-root, real btrfs generation/find-new, real metadata and ACLs over SSH. It is the gate that says Phase 1 actually works.

Safety: the test configures folder_sync to mirror a dedicated test directory (not the real /home/root), so the destructive `--delete` mirror cannot harm VM data — this also exercises the generic any-path mechanism (D-02).

Output: `tests/integration/test_folder_sync.py` with the round-trip, exclusions, metadata, divergence, and dry-run scenarios.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md

@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

@.planning/ROADMAP.md

@.planning/STATE.md

@.planning/phases/01-home-sync-mvp-user-data-sync/01-CONTEXT.md
</context>

## Artifacts this phase produces

This plan creates:
- File `tests/integration/test_folder_sync.py` with the A→B / mutate / B→A round-trip, exclusion, metadata, divergence, and dry-run integration scenarios

<tasks>

<task type="auto">
  <name>Task 1: A→B sync — content, metadata, and exclusion assertions (criteria 1-3)</name>
  <files>tests/integration/test_folder_sync.py</files>
  <read_first>
    - tests/integration/conftest.py (fixtures: pc1_with_pcswitcher_mod, pc2_with_pcswitcher, reset_pcswitcher_state, pc1_executor, pc2_executor; the install-from-current-branch behavior)
    - tests/integration/test_end_to_end_sync.py (how a test invokes `pc-switcher sync <target>` over SSH and asserts exit code; config setup pattern)
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-PATTERNS.md §"tests/integration/test_folder_sync.py" (fixture chain, reset pattern, invoking sync, metadata/checksum verification)
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-RESEARCH.md §"Integration Test: Success Criterion 5" (the exact scenario steps and the md5sum/stat/getfacl commands)
    - docs/dev/testing-guide.md §"Writing Integration Tests" (cleanup in try/finally, unique names, command grouping)
    - src/pcswitcher/default-config.yaml (the folder_sync defaults from plan 02; the test writes a config overriding the folders to a dedicated test path while keeping the same exclude patterns)
  </read_first>
  <action>
    Create `tests/integration/test_folder_sync.py` with a module docstring stating the scenarios covered and VM requirements. Implement the A→B scenario (criteria 1-3), per RESEARCH §"Integration Test":
    - Use `pc1_with_pcswitcher_mod`, `pc2_with_pcswitcher`, `reset_pcswitcher_state`, `pc1_executor`, `pc2_executor`.
    - Write a pc-switcher config on pc1 that enables `sync_jobs.folder_sync` and configures a SINGLE folder entry pointing at a dedicated test directory under a btrfs subvolume (e.g. a unique path under /home so it lives on @home), with the default-style excludes (`.ssh/id_*`, `.config/tailscale`, a VS Code cache dir). Do NOT point it at the real /home or /root.
    - Seed a test tree on pc1 under that directory: regular files with known content; varied ownership (numeric uid/gid different from the login user, set via sudo chown), permissions (e.g. 600/640/755), at least one POSIX ACL (`setfacl`), an mtime set with `touch -d`, a hard-link pair, a symlink; plus files that MUST be excluded (`.ssh/id_rsa`, `.config/tailscale/tailscaled.state`, a VS Code cache dir file) and files that MUST be included (a `.config/Code/User/settings.json`, a dev-tool cache file under e.g. `.cache/uv`). Group seeding into few `run_command` calls (testing-guide perf rule).
    - Run `pc-switcher sync <pc2-host>` from pc1 (login_shell=True); assert exit code 0.
    - Assert on pc2: each included file exists and is byte-identical via `md5sum` (compare to the pc1 value); metadata matches via `stat -c '%u %g %a'` (numeric uid/gid — proves --numeric-ids) and mtime via `stat -c '%Y'`; ACLs match via `getfacl`; the hard-link pair shares an inode (`stat -c '%i'` equal) and the symlink is preserved.
    - Assert the excluded files are ABSENT on pc2 (`test ! -e`), and the VS Code `User/` file and dev-tool cache file are PRESENT.
    - Clean up the test tree and config on both VMs in try/finally.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_folder_sync.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_folder_sync.py` collects without import/syntax errors (`--collect-only` lists the A→B test).
    - The A→B test configures a dedicated test folder (not /home or /root) and asserts: byte-identical content (md5sum), numeric uid/gid + perms + mtime + ACL preservation, hard-link inode sharing, symlink preservation.
    - The test asserts `.ssh/id_rsa`, `.config/tailscale/...`, and a VS Code cache-dir file are absent on the target; the VS Code `User/` file and a dev-tool cache file are present.
    - Running the full VM suite (phase gate) passes the A→B test: `tests/run-integration-tests.sh tests/integration/test_folder_sync.py`.
  </acceptance_criteria>
  <done>The A→B integration test proves content byte-identity, metadata preservation, and that machine-specific items are excluded while VS Code User/ state and dev caches are synced.</done>
</task>

<task type="auto">
  <name>Task 2: Round-trip, deletion propagation, divergence guard, and dry-run (criterion 4 + D-06/D-12)</name>
  <files>tests/integration/test_folder_sync.py</files>
  <read_first>
    - tests/integration/test_folder_sync.py (the A→B scenario + helpers from Task 1 to reuse)
    - tests/integration/conftest.py (reset_pcswitcher_state, pc2_with_pcswitcher for the back-sync source)
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-RESEARCH.md §"Integration Test" steps 3-6 (mutate-on-B, B→A, divergence detection, dry-run) and §"Pitfall 4"
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-CONTEXT.md (D-06 divergence + confirmation, D-07 no false positive on consecutive source syncs, D-12 dry-run)
  </read_first>
  <action>
    Extend `tests/integration/test_folder_sync.py` with the reverse and safety scenarios, per RESEARCH steps 3-6:
    - Round-trip (criterion 4): after an A→B sync, MUTATE the test tree on pc2 — add a new file, modify an existing file's content, and delete a file. Run `pc-switcher sync <pc1-host>` from pc2 (the back-sync; pc2 is now source). Assert pc1 reflects all three mutations: new file present + byte-identical, modified file's new content (md5sum), deleted file absent on pc1 (deletion propagated by `--delete`). Assert metadata is preserved on the reverse direction and the same exclusions hold (an excluded file created on pc2 does not appear on pc1).
    - No-false-divergence (D-07): the legitimate A→B then (after a real back-sync) A→B-again flow must NOT be reported as divergence. Structure the sequence so a normal round trip does not trip the guard.
    - Divergence detection (D-06): independently modify the test tree on the target WITHOUT syncing, then attempt the next `pc-switcher sync <target>`; assert it is BLOCKED — non-zero exit and a divergence message in stdout/stderr (validation aborts before transfer). Then re-run with `--allow-divergence` and assert it proceeds (exit 0) and the target is reconciled.
    - Dry-run (D-12): with a divergence present, run `pc-switcher sync <target> --dry-run`; assert no files changed on the target (compare md5sums before/after), the sync-history/divergence marker is unchanged, and the divergence is logged as a warning while the run still reports what WOULD transfer.
    - Use md5sum / stat / getfacl as in Task 1; group SSH commands; clean up in try/finally.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_folder_sync.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `--collect-only` lists the round-trip, divergence, and dry-run tests with no import/collection error.
    - The round-trip test asserts additions, modifications, AND a deletion propagate B→A byte-identically with metadata preserved and exclusions honored in reverse.
    - The divergence test asserts an independently-modified target blocks the next sync (non-zero exit + divergence message) and that `--allow-divergence` then proceeds.
    - The dry-run test asserts target md5sums are unchanged and the divergence marker is not updated, while a preview/warning is produced.
    - Phase gate: `tests/run-integration-tests.sh tests/integration/test_folder_sync.py` passes the full scenario set.
  </acceptance_criteria>
  <done>The integration suite proves the bidirectional round-trip with deletion propagation, the divergence guard blocking + override, and the no-op dry-run — completing success criterion 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| -------- | ----------- |
| test harness → VMs (SSH) | The test drives real sudo/rsync/btrfs on pc1/pc2 over SSH. |
| security behavior ← assertions | This test is the executable proof of the exclusion and deletion-safety properties from plans 02/04/05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-06-01 | Information disclosure | secret-exclusion behavior | high | mitigate | The test asserts `.ssh/id_*` and `.config/tailscale` are absent on the target after sync in BOTH directions — the executable guarantee for success criteria 3 & 4. |
| T-06-02 | Tampering / Data loss | mirror-delete on test data | medium | mitigate | The test mirrors a dedicated test directory (not real /home or /root) and cleans up in try/finally, so `--delete` cannot harm VM or developer data. |
| T-06-03 | Integrity | divergence-guard regression | high | mitigate | The test asserts an independently-modified target is blocked and that a normal round trip is NOT false-flagged (D-07), catching marker-erasure or detection regressions on real btrfs. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; the test reuses existing infra and standard system tools (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_folder_sync.py --collect-only -q` lists all scenarios with no collection error.
- Phase gate (requires VMs + pushed branch): `tests/run-integration-tests.sh tests/integration/test_folder_sync.py` is green — this is the success-criterion-5 metric.
- `uv run ruff check tests/integration/test_folder_sync.py && uv run basedpyright tests/integration/test_folder_sync.py` report no errors.
</verification>

<success_criteria>
A VM-isolated integration test automates A→B → mutate-on-B → B→A and asserts byte-identical content, preserved metadata, honored exclusions both directions, propagated deletions, the divergence guard blocking + override, and a no-op dry-run — proving ROADMAP success criteria 1-5.
</success_criteria>

<output>
Create `.planning/phases/01-home-sync-mvp-user-data-sync/01-06-SUMMARY.md` when done.
</output>
