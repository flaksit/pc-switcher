---
phase: 01-home-sync-mvp-user-data-sync
plan: 14
type: execute
wave: 4
depends_on:
  - 01-11
  - 01-13
files_modified:
  - README.md
  - tests/unit/jobs/test_folder_sync_deletion_log.py
  - tests/integration/test_folder_sync.py
autonomous: true
gap_closure: true
requirements:
  - REQ-terminal-ux
  - REQ-sync-scope-file-metadata
  - REQ-sync-scope-user-data
user_setup: []

must_haves:
  truths:
    - "README 'What Happens During a Sync' describes the topology out-of-order/target-state step after the target lock, no longer lists the consecutive-sync check or the btrfs validate() divergence guard, and documents --allow-out-of-order (not --allow-consecutive/--allow-divergence)."
    - "rsync deletions (*deleting <path>) are persisted to the JSON-lines log at FULL level for BOTH dry-run and real runs, proven by a test that reads the written log file."
    - "The integration round-trip test matches the new model: it uses --allow-out-of-order where there is no prior clean history and asserts the legit back-and-forth proceeds without any override; no --allow-divergence remains."
  artifacts:
    - "README.md — corrected sync-sequence section"
    - "tests/unit/jobs/test_folder_sync_deletion_log.py — FULL-log deletion-persistence test (dry-run + real)"
    - "tests/integration/test_folder_sync.py — divergence scenarios reworked to the topology model"
  key_links:
    - "_stream_rsync FULL log line for *deleting -> logger file handler (file level DEBUG <= FULL) -> persisted JSON-lines log."
  prohibitions:
    - "No --allow-divergence usage or btrfs-find-new divergence assertions may remain in tests/integration/test_folder_sync.py or README.md."
---

<objective>
Close out the pivot's documentation and observability: correct the README sync narrative, prove the deletion audit trail is persisted at FULL for both dry-run and real runs, and rework the integration test's divergence scenarios to the topology model.

Purpose: The new safety story (ADR-015) leans on the dry-run deletion log as the user-facing "what would be destroyed" preview and on the topology confirmation; both must be documented and tested, and the integration test must stop asserting the removed btrfs guard.

Output: Accurate README, a deletion-log persistence test, and a topology-aligned integration test.
</objective>

<artifacts_produced>
- Corrected README.md "What Happens During a Sync".
- New `tests/unit/jobs/test_folder_sync_deletion_log.py`.
- Reworked `tests/integration/test_folder_sync.py`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/adr/adr-015-topology-based-sync-safety-model.md
@README.md
@src/pcswitcher/jobs/folder_sync.py
@src/pcswitcher/logger.py
@src/pcswitcher/default-config.yaml
@tests/integration/test_folder_sync.py
</context>

<tasks>

<!-- planner-discipline-allow: allow-divergence -->

<task type="auto">
  <name>Task 1: Correct the README sync sequence</name>
  <files>README.md</files>
  <read_first>
    - README.md ("## What Happens During a Sync" lines 74-99, especially step 2 consecutive-sync check line 81, step 5 target lock line 84, step 6 divergence-guard note lines 85-89, and the closing --allow-divergence sentence line 99)
    - docs/adr/adr-015-topology-based-sync-safety-model.md (the new step + safety model)
    - src/pcswitcher/orchestrator.py (final run() sequence after plan 01-13)
  </read_first>
  <action>
    Update "## What Happens During a Sync" so the numbered sequence matches the post-pivot orchestrator: remove the old step 2 "Consecutive-sync check"; after "Acquire target lock" insert a new step describing the out-of-order / target-state check (reads this machine's and the target's sync-history last_role+last_peer, warns and asks for confirmation when the target may hold independent state — e.g. the target last synced with a different machine, or no history exists — and is a confirmable heads-up, not a hard abort for the legit back-and-forth; skipped with --allow-out-of-order); renumber the following steps accordingly. In the "Discover & validate jobs" step, delete the paragraph claiming the folder-sync divergence guard runs via `btrfs subvolume find-new` in validate() — validate() now only checks sudo rsync, acl, and folder existence. Update the closing note: replace the --allow-divergence sentence with one that says --allow-out-of-order skips the target-state confirmation, and mention that rsync --dry-run lists the exact files/deletions that would occur and that deletions are recorded in the FULL-level log. Follow project markdown rules (no `---` separators, no bold-as-heading, one paragraph per line). Run codespell on README.md.
  </action>
  <verify>
    <automated>grep -q "allow-out-of-order" README.md && ! grep -nE "allow-divergence|allow-consecutive|Consecutive-sync check|find-new" README.md</automated>
  </verify>
  <acceptance_criteria>
    - The sync sequence includes the target-state/out-of-order step after the target lock and omits the consecutive-sync check.
    - No mention of the btrfs find-new validate() guard, --allow-divergence, or --allow-consecutive remains.
    - --allow-out-of-order and the dry-run deletion log are documented.
  </acceptance_criteria>
  <done>The README narrative matches the topology safety model.</done>
</task>

<task type="auto">
  <name>Task 2: Prove deletions persist at FULL for dry-run and real runs</name>
  <files>tests/unit/jobs/test_folder_sync_deletion_log.py</files>
  <read_first>
    - src/pcswitcher/jobs/folder_sync.py (_stream_rsync per-file FULL logging at the `*deleting` branch, and how execute() streams both dry-run and real output)
    - src/pcswitcher/logger.py (setup_logging, file handler level = log_config.file, JsonFormatter, QueueListener)
    - src/pcswitcher/default-config.yaml (logging.file default DEBUG=10, which is <= FULL=15)
    - src/pcswitcher/models.py (LogLevel.FULL = 15)
    - tests/unit/jobs/test_folder_sync.py (TestStreamRsync fixtures/fake async chunk generator pattern to reuse)
  </read_first>
  <action>
    Create a new unit test module that proves a deletion line reaches the persisted JSON-lines log at FULL. Set up real logging with setup_logging(tmp_path log file, a LogConfig whose file floor is DEBUG so FULL passes) and, after exercising the code, stop the QueueListener so records are flushed before reading the file. Drive FolderSyncJob._stream_rsync with a fake async byte-chunk generator whose output contains at least one `*deleting <path>` line (reuse the TestStreamRsync fake-generator pattern). Then read the log file and assert a JSON line exists whose level is FULL and whose message contains the deleted path. Add two cases proving persistence is independent of dry-run: one with a JobContext where dry_run is True and one where dry_run is False (both stream the same `*deleting` line through _stream_rsync, since execute() streams output in both modes). Also assert the default file log level (from default-config.yaml or LogConfig default) is at or below FULL so deletions are captured by default. Keep the test hermetic (tmp_path only; no network). Run `uv run pytest tests/unit/jobs/test_folder_sync_deletion_log.py -q`, ruff, basedpyright, codespell.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/jobs/test_folder_sync_deletion_log.py -q && uv run basedpyright tests/unit/jobs/test_folder_sync_deletion_log.py</automated>
  </verify>
  <acceptance_criteria>
    - The test writes a real log file, then asserts a FULL-level JSON record containing the deleted path is present.
    - Both the dry-run and real-run streaming cases assert persistence.
    - An assertion confirms the default file log floor is <= FULL.
    - Test passes and basedpyright is clean.
  </acceptance_criteria>
  <done>The deletion audit trail is proven to persist at FULL in both modes.</done>
</task>

<task type="auto">
  <name>Task 3: Rework the integration divergence scenarios</name>
  <files>tests/integration/test_folder_sync.py</files>
  <read_first>
    - tests/integration/test_folder_sync.py (module docstring lines 1-20; TestFolderSyncAToB line 178; TestFolderSyncRoundTrip line 338; the no-false-divergence round-trip test lines 341-517; the divergence-guard + dry-run test lines 522-686 with its --allow-divergence step ~661-668)
    - docs/adr/adr-015-topology-based-sync-safety-model.md (new model)
    - src/pcswitcher/orchestrator.py (out-of-order step + --allow-out-of-order after plan 01-13)
  </read_first>
  <action>
    Update the integration test to the topology model. Keep TestFolderSyncAToB::test_a_to_b_content_metadata_and_exclusions but ensure the initial A→B baseline sync uses `--allow-out-of-order` (the target has no prior clean history, so the new step would otherwise warn/abort non-interactively). Keep the byte-identity, metadata, and exclusion assertions unchanged (these prove REQ-sync-scope-user-data / -file-metadata / machine-specific-exclusions).

    For TestFolderSyncRoundTrip::test_round_trip_and_no_false_divergence: use --allow-out-of-order only on the first sync that has no prior history; then assert the legit back-and-forth (A→B, mutate B, B→A, A→B again) proceeds WITHOUT any override and WITHOUT an out-of-order/overwrite warning in output (this is the #159 property: the clean case where the target last synced with this source is silent). Add an assertion that a deleted file is reported in the run's FULL log (the dry-run/real deletion audit trail) if the test already inspects logs; otherwise assert deletion propagation as before.

    Replace the old TestFolderSyncRoundTrip::test_divergence_guard_and_dry_run: remove all --allow-divergence usage and btrfs-find-new divergence expectations. Repurpose it to exercise the new model at integration level: (1) a machine-C / no-prior-history situation triggers the out-of-order heads-up in non-interactive mode (sync without --allow-out-of-order returns non-zero and the output mentions the out-of-order/target-state reason), and (2) `--allow-out-of-order` proceeds (exit 0) and reconciles the target; and (3) `--dry-run` performs a read-only preview that lists would-be deletions and makes no changes (no history update, files unchanged). Update the module docstring to describe the topology model instead of the btrfs divergence guard. Keep the tests marked as integration (same markers/fixtures). Run ruff/codespell; do not attempt to run the live VM suite here.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_folder_sync.py --collect-only -q && ! grep -nE "allow-divergence|allow_divergence|find-new" tests/integration/test_folder_sync.py && grep -q "allow-out-of-order" tests/integration/test_folder_sync.py</automated>
  </verify>
  <acceptance_criteria>
    - The integration module collects without error offline.
    - No --allow-divergence or find-new references remain; --allow-out-of-order is used for baseline/override.
    - The round-trip test asserts the legit back-and-forth needs no override; the reworked scenario covers the out-of-order heads-up, the override, and dry-run preview.
  </acceptance_criteria>
  <done>The integration test validates the topology model and drops the removed guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync run → audit log | the FULL-level log is the durable record of what was deleted |
| docs → operator | README is the operator's mental model of what a sync does to the target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14-01 | Repudiation | deletion audit trail | medium | mitigate | Test proves `*deleting` lines persist at FULL in the JSON-lines file for both dry-run and real runs, so destructive actions are always auditable — a core replacement for the removed guard. |
| T-01-14-02 | Information / mismodel | stale README | low | mitigate | README updated to the real sequence so operators understand the confirmation gate and dry-run preview rather than relying on a guard that no longer exists. |
</threat_model>

<verification>
- README "What Happens During a Sync" reflects the topology step, omits the consecutive/find-new guards, and documents --allow-out-of-order + the dry-run deletion log.
- `uv run pytest tests/unit/jobs/test_folder_sync_deletion_log.py` passes; deletions persist at FULL in both dry-run and real streaming.
- tests/integration/test_folder_sync.py collects offline, uses --allow-out-of-order, and contains no --allow-divergence/find-new.
</verification>

<success_criteria>
The documentation and tests match the topology safety model: the deletion audit trail is proven durable, the README is accurate, and the integration test validates the new confirmation flow instead of the removed btrfs guard.
</success_criteria>

<output>
Create `.planning/phases/01-home-sync-mvp-user-data-sync/01-14-SUMMARY.md` when done.
</output>