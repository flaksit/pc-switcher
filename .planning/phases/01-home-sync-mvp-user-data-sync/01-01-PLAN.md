---
phase: 01-home-sync-mvp-user-data-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/adr-013-rsync-over-ssh-user-data-transport.md
  - docs/adr/adr-014-unified-dry-run-contract.md
  - docs/adr/_index.md
autonomous: true
requirements:
  - REQ-sync-scope-user-data
  - REQ-manual-sync-workflow
user_setup: []

must_haves:
  truths:
    - "ADR-013 records rsync-over-SSH as the user-data transport, running as root via sudo on both ends, with root SSH login forbidden (D-04, D-05)."
    - "ADR-014 records the tool-wide dry-run contract every SyncJob must honor (D-12)."
    - "docs/adr/_index.md lists ADR-013 and ADR-014 as Active Decisions."
  artifacts:
    - docs/adr/adr-013-rsync-over-ssh-user-data-transport.md
    - docs/adr/adr-014-unified-dry-run-contract.md
  key_links:
    - "ADR-013 references and is consistent with ADR-002 (SSH channel) and ADR-005 (asyncio)."
  prohibitions:
    - "ADRs MUST NOT be edited after acceptance — they are immutable (ADR-001); these are net-new records."
    - "ADR-013 MUST NOT propose enabling root SSH login."
---

<objective>
Formalize the three ADR-worthy Phase 1 decisions as immutable Architecture Decision Records so the locked decisions have a canonical source. Two ADRs (tightly-coupled decisions combined per ADR-001 granularity guidance):
- ADR-013: rsync-over-SSH as the user-data transport, running as root via sudo on both ends (D-04 + D-05 — same transport mechanism, decided together).
- ADR-014: the unified `--dry-run` contract for all SyncJobs (D-12 — a separate cross-cutting policy).

Purpose: The bootstrap STATE.md records an explicit blocker — "the rsync-over-SSH transport choice is not yet captured in an ADR." This plan closes that and the related dry-run/root-via-sudo gaps before the job is built, so executors of later plans implement against a locked decision.

Output: Two new ADR files and an updated ADR index.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md

@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

@.planning/ROADMAP.md

@.planning/STATE.md

@.planning/phases/01-home-sync-mvp-user-data-sync/01-CONTEXT.md
</context>

## Artifacts this phase produces

This plan creates:
- File `docs/adr/adr-013-rsync-over-ssh-user-data-transport.md` (ADR-013)
- File `docs/adr/adr-014-unified-dry-run-contract.md` (ADR-014)
- Two new entries in `docs/adr/_index.md`

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-013 — rsync-over-SSH user-data transport (root via sudo)</name>
  <files>docs/adr/adr-013-rsync-over-ssh-user-data-transport.md</files>
  <read_first>
    - docs/adr/adr-001-adr.md (ADR document structure + Implementation Rules + immutability; note the index filename convention is `adr-NNN-slug.md`)
    - docs/adr/adr-002-ssh-communication-channel.md (the SSH channel this transport rides on; "File syncing protocols (rsync, rclone, custom) are invoked through this SSH communication channel")
    - docs/adr/adr-005-asyncio-concurrency.md (rsync must be driven via async subprocess; no blocking calls)
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-CONTEXT.md (D-04, D-05, D-13)
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-RESEARCH.md §"Pattern 1: rsync Invocation (Root on Both Ends)", §"Security Domain", §"Common Pitfalls" (Pitfall 1, Pitfall 3)
  </read_first>
  <action>
    Create `docs/adr/adr-013-rsync-over-ssh-user-data-transport.md` following the exact ADR document structure from ADR-001 (Status: Accepted; Date: 2026-06-30; TL;DR; Implementation Rules; Context under 200 words; Decision; Consequences Positive/Negative; References). Capture, per D-04 and D-05:
    - Decision: rsync-over-SSH is the user-data transport (chosen over btrfs send/receive); rsync runs as root on BOTH ends — local `sudo rsync` on source and `--rsync-path="sudo rsync"` on the target, reached over the existing normal-user SSH connection. Root is required to read/write files of all owners across /home and /root and to preserve owner/group.
    - Implementation Rules (Required): always pass `--numeric-ids` for cross-machine uid/gid fidelity; baseline flags `-aAXHS` (archive, ACLs, xattrs, hard links, sparse) per D-13; rsync is driven via asyncio subprocess (ADR-005); rsync uses its own SSH transport via `-e ssh` honoring the user's `~/.ssh/config` (ADR-002 hostname model); preserve the SSH identity under sudo (root's environment differs — see RESEARCH Pitfall 1).
    - Implementation Rules (Forbidden): do NOT enable root SSH login (keep the attack surface small — the SSH login stays as the normal user, privilege is escalated only for the rsync binary via sudo); do NOT use a blocking rsync call.
    - Consequences: note the sudoers prerequisite (passwordless sudo scoped to the rsync binary path on both machines) and that reflink/shared-extent topology is not preserved (accepted tradeoff, D-13).
    - References: ADR-002, ADR-005, D-04, D-05, D-13, RESEARCH.md Pattern 1.
    Keep it concise per ADR-001. Do not duplicate prose across sections (only TL;DR repeats).
  </action>
  <verify>
    <automated>test -f docs/adr/adr-013-rsync-over-ssh-user-data-transport.md && grep -q "^Status: Accepted" docs/adr/adr-013-rsync-over-ssh-user-data-transport.md && grep -q "rsync-path" docs/adr/adr-013-rsync-over-ssh-user-data-transport.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/adr/adr-013-rsync-over-ssh-user-data-transport.md` exists and contains the ADR-001 section headings: `## TL;DR`, `## Implementation Rules`, `## Context`, `## Decision`, `## Consequences`, `## References`.
    - The ADR states root is obtained via `--rsync-path="sudo rsync"` on the target and local `sudo rsync` on the source, and explicitly forbids enabling root SSH login.
    - The ADR cites D-04 and D-05 and references ADR-002 and ADR-005.
  </acceptance_criteria>
  <done>ADR-013 exists, follows the ADR-001 structure, and records the rsync-over-SSH root-via-sudo transport decision with D-04/D-05 traceability.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-014 (unified dry-run contract) and update the ADR index</name>
  <files>docs/adr/adr-014-unified-dry-run-contract.md, docs/adr/_index.md</files>
  <read_first>
    - docs/adr/adr-001-adr.md (ADR structure + rule: keep _index.md as a list of all and only current ADRs)
    - docs/adr/_index.md (current index; append entries in numeric order)
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-CONTEXT.md (D-12)
    - src/pcswitcher/jobs/context.py (JobContext.dry_run already exists)
    - src/pcswitcher/orchestrator.py lines 204-279 (where dry-run is honored today and where sync-history update happens unconditionally)
  </read_first>
  <action>
    Create `docs/adr/adr-014-unified-dry-run-contract.md` per the ADR-001 structure capturing D-12: `--dry-run` is a tool-wide contract binding on ALL SyncJobs (and the orchestrator), not just folder_sync. Record the Implementation Rules:
    - A dry run performs a full read-only preview: connect, lock, validate, and run any divergence/conflict detection — every job reports exactly what it WOULD do.
    - Forbidden in dry-run: writing files, creating btrfs snapshots, updating sync-history, updating any per-target divergence marker.
    - Every SyncJob MUST implement a real preview (no-op stubs are non-compliant); folder_sync satisfies this with `rsync --dry-run`.
    - The orchestrator MUST skip the post-sync sync-history update when dry_run is set (this plan's sibling plan 03 implements that change; reference it as the enforcement point).
    Then update `docs/adr/_index.md`: add two entries under "## Active Decisions" in numeric order — ADR-013 (rsync-over-SSH user-data transport, root via sudo) and ADR-014 (unified dry-run contract). Bump the "Last updated" date to 2026-06-30.
  </action>
  <verify>
    <automated>test -f docs/adr/adr-014-unified-dry-run-contract.md && grep -q "ADR-013" docs/adr/_index.md && grep -q "ADR-014" docs/adr/_index.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/adr/adr-014-unified-dry-run-contract.md` exists with the ADR-001 section headings and cites D-12.
    - ADR-014 lists the forbidden dry-run operations (no file writes, no snapshots, no sync-history update, no divergence-marker update).
    - `docs/adr/_index.md` contains `[ADR-013]` and `[ADR-014]` link entries under Active Decisions, in numeric order, each pointing at the correct filename.
  </acceptance_criteria>
  <done>ADR-014 exists and the index lists both new ADRs with correct links and an updated date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source→target (SSH) | rsync rides the SSH channel (ADR-002); orchestration commands cross here. |
| user→sudo (privilege escalation) | rsync is run as root via sudo on both ends. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of privilege | Documented transport model | medium | mitigate | ADR-013 mandates sudo scoped to the rsync binary path and forbids enabling root SSH login, constraining how downstream plans wire privilege. |
| T-01-02 | Repudiation | Decision provenance | low | mitigate | ADRs are immutable (ADR-001); the locked transport/dry-run decisions get a durable, traceable record. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed in this plan (docs only); RESEARCH Package Legitimacy Audit confirms no new dependencies in this phase. |
</threat_model>

<verification>
- `ls docs/adr/adr-013-*.md docs/adr/adr-014-*.md` lists both files.
- `grep -c "^# ADR-01" docs/adr/adr-013-*.md docs/adr/adr-014-*.md` confirms both have ADR titles.
- Both ADRs follow the ADR-001 section structure (TL;DR, Implementation Rules, Context, Decision, Consequences, References).
</verification>

<success_criteria>
ADR-013 and ADR-014 exist as immutable records, follow the ADR-001 structure, capture D-04/D-05 and D-12 respectively, and are listed in `docs/adr/_index.md`.
</success_criteria>

<output>
Create `.planning/phases/01-home-sync-mvp-user-data-sync/01-01-SUMMARY.md` when done.
</output>
