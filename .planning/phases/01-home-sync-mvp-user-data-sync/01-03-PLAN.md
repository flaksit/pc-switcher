---
phase: 01-home-sync-mvp-user-data-sync
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pcswitcher/jobs/context.py
  - src/pcswitcher/cli.py
  - src/pcswitcher/orchestrator.py
  - src/pcswitcher/sync_history.py
  - tests/unit/test_dry_run.py
  - tests/unit/test_sync_history.py
autonomous: true
requirements:
  - REQ-manual-sync-workflow
user_setup: []

must_haves:
  truths:
    - "JobContext carries allow_divergence (frozen field, default False), plumbed from a new sync --allow-divergence CLI flag through the Orchestrator (mirrors the existing dry_run plumbing) — the override path for D-06."
    - "On a dry run the orchestrator does NOT update sync-history (D-12)."
    - "sync_history exposes get_target_generation(target_hostname, path) and set_target_generation(target_hostname, path, generation), persisting a target_generations map alongside last_role (D-08)."
    - "Recording last_role (local record_role and the remote role-record command) preserves any existing target_generations map rather than overwriting the file."
  artifacts:
    - src/pcswitcher/sync_history.py
    - src/pcswitcher/jobs/context.py
  key_links:
    - "FolderSyncJob (plan 04/05) reads context.allow_divergence in validate() and calls sync_history.get_target_generation/set_target_generation for the divergence guard."
    - "Orchestrator._update_sync_history runs only when not dry_run; record_role merges into the same file folder_sync writes target_generations to."
  prohibitions:
    - "A dry run MUST NOT write sync-history.json or any divergence marker (D-12)."
    - "Updating last_role MUST NOT erase target_generations on either machine (prevents the Pitfall 4 false-divergence after a role switch)."
---

<objective>
Build the cross-cutting state and flags the divergence guard and the unified dry-run contract depend on, without yet touching the job:
- `JobContext.allow_divergence` + a `pc-switcher sync --allow-divergence` flag, plumbed like the existing `dry_run` — the explicit-confirmation override for D-06 (re-run with the flag after manual review, consistent with `--allow-consecutive`).
- The D-12 orchestrator gap: skip the post-sync sync-history update in dry-run.
- The per-target divergence-marker store in `sync_history.py` (D-08): the current file records only `last_role`, which is insufficient. Add a `target_generations` map and make every `last_role` write merge-preserving so markers survive role switches (the Pitfall 4 trap).

Purpose: These are shared by the job (plan 04/05) and must exist first. Getting the merge-preserving writes right here is what makes the bidirectional round-trip (success criteria 4 & 5) detect real divergence without false positives.

Output: A plumbed `allow_divergence` flag, a dry-run-safe history update, and a backward-compatible divergence-marker store with merge-preserving writes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md

@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

@.planning/ROADMAP.md

@.planning/STATE.md

@.planning/phases/01-home-sync-mvp-user-data-sync/01-CONTEXT.md
</context>

## Artifacts this phase produces

This plan creates:
- Field `JobContext.allow_divergence` (bool, default False)
- CLI flag `pc-switcher sync --allow-divergence`
- Orchestrator param `allow_divergence`
- Functions `sync_history.get_target_generation`, `sync_history.set_target_generation`
- Sync-history JSON key `target_generations` (map: target_hostname → { path → generation })

<tasks>

<task type="auto">
  <name>Task 1: Plumb allow_divergence + make dry-run skip the sync-history update</name>
  <files>src/pcswitcher/jobs/context.py, src/pcswitcher/cli.py, src/pcswitcher/orchestrator.py, tests/unit/test_dry_run.py</files>
  <read_first>
    - src/pcswitcher/jobs/context.py (frozen JobContext dataclass; `dry_run: bool = False` is the field to mirror)
    - src/pcswitcher/cli.py lines 182-333 (the `sync` command options, `_run_sync`, `_async_run_sync`; mirror the `--allow-consecutive` / `dry_run` threading into `Orchestrator(...)`)
    - src/pcswitcher/orchestrator.py lines 77-142 (constructor params and `_create_job_context`), lines 272-279 (the unconditional `_update_sync_history()` call on success), lines 452-474 (`_update_sync_history`)
    - tests/unit/test_dry_run.py (existing dry-run propagation tests to extend; `TestOrchestratorDryRunPropagation`, `TestJobContextDryRunField`)
  </read_first>
  <action>
    Per D-06 (override path) and D-12 (dry-run preview):
    - In `context.py`, add `allow_divergence: bool = False` to `JobContext` (after `dry_run`, keeping the frozen dataclass).
    - In `cli.py`, add an `--allow-divergence` Typer option to `sync` (help: skip-on-confirm override for the target-divergence guard; default False), and thread it through `_run_sync` and `_async_run_sync` into `Orchestrator(...)` exactly as `allow_consecutive`/`dry_run` are threaded.
    - In `orchestrator.py`, add an `allow_divergence: bool = False` keyword param to `__init__`, store `self._allow_divergence`, and pass it into the `JobContext(...)` built by `_create_job_context`.
    - In `orchestrator.py`, guard the success-path `await self._update_sync_history()` call so it runs only `if not self._dry_run` (D-12: dry-run must not update history). Leave the body of `_update_sync_history` otherwise unchanged here.
    - Extend `tests/unit/test_dry_run.py`: add a test that `JobContext` has `allow_divergence` defaulting to False; add a test that `Orchestrator.__init__` accepts an `allow_divergence` parameter (mirror `test_orchestrator_accepts_dry_run_parameter`).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dry_run.py -q && uv run python -c "import inspect; from pcswitcher.orchestrator import Orchestrator; from pcswitcher.jobs.context import JobContext; assert 'allow_divergence' in inspect.signature(Orchestrator.__init__).parameters; import dataclasses; f={x.name:x for x in dataclasses.fields(JobContext)}; assert f['allow_divergence'].default is False; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `JobContext` is constructible with `allow_divergence=True` and defaults to `False`.
    - `Orchestrator.__init__` accepts `allow_divergence`; `_create_job_context` passes it into every JobContext it builds.
    - `pc-switcher sync --help` lists `--allow-divergence`.
    - The success-path `_update_sync_history()` call is wrapped in `if not self._dry_run:`.
    - `tests/unit/test_dry_run.py` passes including the two new tests.
  </acceptance_criteria>
  <done>allow_divergence is plumbed CLI→Orchestrator→JobContext like dry_run, and a dry run no longer updates sync-history.</done>
</task>

<task type="auto">
  <name>Task 2: Add the target_generations divergence-marker store with merge-preserving writes</name>
  <files>src/pcswitcher/sync_history.py, tests/unit/test_sync_history.py</files>
  <read_first>
    - src/pcswitcher/sync_history.py (whole file: `record_role` atomic temp+rename write at lines 102-133, `get_record_role_command` remote echo at lines 136-149, `get_history_path`, `get_last_role_with_error`)
    - tests/unit/test_sync_history.py (existing tests + how get_history_path is patched/HOME is set)
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-PATTERNS.md §"src/pcswitcher/sync_history.py" (backward-compatible extension + atomic-write pattern)
    - .planning/phases/01-home-sync-mvp-user-data-sync/01-RESEARCH.md §"Pattern 3" (the target_generations JSON shape) and §"Pitfall 4" (asymmetric marker update)
  </read_first>
  <action>
    Extend `sync_history.py` for the per-target divergence marker (D-08), keeping the file backward-compatible (old files with only `last_role` keep working; missing keys read as `None`):
    - Add `get_target_generation(target_hostname: str, path: str) -> int | None` — reads `sync-history.json`, returns `target_generations[target_hostname][path]` or `None` if the file/keys are absent or the file is corrupt.
    - Add `set_target_generation(target_hostname: str, path: str, generation: int) -> None` — read-modify-write: load existing JSON (treat missing/corrupt as `{}`), set the nested value, preserve `last_role` and any other targets/paths, write atomically via the existing temp-file + rename pattern.
    - Make `record_role` merge-preserving: read the existing file first and preserve `target_generations` (and any other keys) when writing the new `last_role`. It MUST NOT clobber markers written earlier in the same sync by FolderSyncJob.
    - Make `get_record_role_command` merge-preserving on the remote: the returned shell command must set `last_role` while preserving any existing `target_generations` in the remote file (rather than overwriting with a fresh object). Recommended: a remote read-modify-write (the Ubuntu target has python3 available at runtime, and pc-switcher is already installed there); choose a mechanism that leaves a missing file producing just `{"last_role": ...}`.
    - Export the two new functions in `__all__`.
    Add tests to `tests/unit/test_sync_history.py`: round-trip set/get for one and multiple (target, path) pairs; `get_target_generation` returns None for a missing file and for an old-format file with only `last_role`; `set_target_generation` preserves an existing `last_role`; `record_role` preserves an existing `target_generations` map; and a test that executes the string from `get_record_role_command` (e.g. via subprocess with a temp HOME and a pre-seeded file) and asserts the resulting file has the new `last_role` AND the preserved `target_generations`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_sync_history.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `get_target_generation('b','/home')` returns `None` when no marker exists and the stored int after `set_target_generation('b','/home', 54321)`.
    - After `set_target_generation`, a pre-existing `last_role` value is unchanged.
    - After `record_role(SyncRole.SOURCE)`, a pre-existing `target_generations` map is still present and unchanged.
    - Executing the command from `get_record_role_command(SyncRole.TARGET)` against a file pre-seeded with a `target_generations` map yields a file whose `last_role` is `target` and whose `target_generations` is preserved.
    - All of `tests/unit/test_sync_history.py` passes.
  </acceptance_criteria>
  <done>sync_history persists per-target per-path generations, and every last_role write (local and remote) preserves them — the marker survives role switches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → divergence guard | The `--allow-divergence` flag lets the user override a detected divergence. |
| source state file ↔ target state file | sync-history.json holds the markers the destructive mirror is gated on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Integrity / Repudiation | `--allow-divergence` override | medium | mitigate | The override is opt-in and explicit; plan 04 logs the override decision at WARNING for an audit trail. The default path blocks. |
| T-03-02 | Integrity (data loss) | Marker erasure on role switch | high | mitigate | record_role and the remote role-record become merge-preserving so a later sync cannot silently skip the divergence check (which would unguard a destructive mirror). Covered by unit tests here and the round-trip in plan 06. |
| T-03-03 | Tampering | Dry-run side effects | medium | mitigate | Orchestrator skips the sync-history update in dry-run (D-12); plan 05/06 assert no marker writes in dry-run. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; RESEARCH Package Legitimacy Audit confirms no new dependencies. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_dry_run.py tests/unit/test_sync_history.py -q` is green.
- `uv run basedpyright src/pcswitcher/sync_history.py src/pcswitcher/jobs/context.py src/pcswitcher/orchestrator.py src/pcswitcher/cli.py` reports no new errors.
- `uv run pytest tests/unit tests/contract -q` stays green (no regression in existing sync-history/orchestrator behavior).
</verification>

<success_criteria>
allow_divergence is plumbed end-to-end, dry-run no longer writes history, and the divergence-marker store persists per-target generations with merge-preserving writes that survive role switches.
</success_criteria>

<output>
Create `.planning/phases/01-home-sync-mvp-user-data-sync/01-03-SUMMARY.md` when done.
</output>
