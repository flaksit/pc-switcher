---
phase: 02-package-management-sync
plan: 21
type: execute
wave: 6
depends_on:
  - 02-19
files_modified:
  - tests/integration/jobs/test_package_sync.py
  - tests/integration/test_config_sync.py
  - .planning/phases/02-package-management-sync/02-VALIDATION.md
autonomous: false
requirements:
  - REQ-sync-scope-packages
  - REQ-conflict-detection-no-resolution
user_setup:
  - service: github-actions-ci
    why: "The VM integration suite runs only in CI on a PR targeting main, gated on draft==false; there is no local VM access (HCLOUD_TOKEN and PC_SWITCHER_TEST_PC{1,2}_HOST are unset)."
    dashboard_config:
      - task: "Mark PR #206 ready-for-review (draft=false) so the integration-tests workflow runs the reworked suite; a full VM run takes ~21 minutes."
        location: "GitHub PR #206 on branch gsd/phase-02-package-management-sync"
must_haves:
  truths:
    - "The integration suite imports the new jobs.packages.* module paths and references the corrected per-manager review, not a coordinator."
    - "A VM-level test exercises apt repository state end to end: a target missing one vendor repo receives the source's source file and its signing key as separate review entries, and the intended apt-get update is reported — closing broken-window ledger entry #2."
    - "A VM-level test enables manual_installs_sync and proves an install snippet authored/registered on the source reaches the target by the job's own push and is replayed there."
    - "The cross-manager ordering test is replaced or reframed: with the coordinator gone, the assertion is that each enabled manager completes its own review before that manager's first mutating command, not that all managers diff before any applies."
    - "Every integration assertion checks the target's own package-manager or filesystem state, never pc-switcher's log text, except where an explicit ordering witness requires the run log."
    - statement: "The reworked suite references no PackagePhaseCoordinator symbol and no SYNCED_CONFIG_FILENAMES-based multi-file config transfer."
      verification: backstop
  prohibitions:
    - "An integration test never asserts a package is installed by reading pc-switcher's log instead of the target's package manager."
    - "A test never leaves pc2's package, repository, snap or flatpak state changed; every mutation is restored in a finally regardless of outcome."
    - "The reworked suite never re-introduces a cross-manager coordinator ordering assertion."
  artifacts:
    - tests/integration/jobs/test_package_sync.py
    - .planning/phases/02-package-management-sync/02-VALIDATION.md
  key_links:
    - "Broken-window ledger entry #2 requires VM coverage of apt repository state (sources.list.d, keyrings) that the suite has never exercised; this plan adds it and the entry is marked fixed only after CI runs it green."
    - "The suite drives reviews non-interactively via PCSWITCHER_PACKAGE_REVIEW_AUTOMATION; that hook still exists and still answers per-manager reviews, so existing automation helpers mostly carry over."
    - "No local VM access means the real proof is a CI run on PR #206 with draft=false; the human checkpoint gates marking ledger #2 fixed on that green run."
---

<objective>
Rework the VM integration suite for the new job and the per-manager review shape, and add the apt-repository-state VM coverage that closes broken-window ledger entry #2.

Purpose: the integration suite was written against the coordinator, three jobs, and the old snippet transport, and its module imports point at paths this delta moved. Separately, ledger entry #2 is genuinely open: it asks for a VM-level dry-run against a target missing one vendor repo, with the key and source shown as separate review entries and the intended `apt-get update` reported. Verified — the VM suite contains no reference to `sources.list.d`, `keyrings`, `trusted.gpg.d`, `preferences.d` or `apt.conf.d`; apt repository state has only ever been exercised in unit tests. `/gsd-ship` blocks while the entry is open.

Output: the suite updated to the new module paths, the four-job set, the per-manager review and the self-pushed snippet transport; a new apt-repository-state VM test and a `manual_installs_sync` VM test; the cross-manager ordering test reframed; and ledger entry #2 marked fixed after the reworked suite runs green in CI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-management-sync/02-CONTEXT.md
@.planning/phases/02-package-management-sync/02-VALIDATION.md
@.planning/WINDOWS.md
</context>

<artifacts_this_phase_produces>
This plan creates no source symbols. It adds integration tests (test functions in `tests/integration/jobs/test_package_sync.py`) and marks broken-window ledger entry #2 fixed via `gsd-tools windows fixed 2`. It references, but does not create: `ManualInstallsSyncJob`, `manual_installs_sync`, the `jobs.packages.*` module paths, and the corrected per-manager review — all produced by earlier plans in this delta.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Retarget the suite to the new modules, jobs and review shape</name>
  <read_first>
    - tests/integration/jobs/test_package_sync.py — read the module docstring, the imports (lines ~40-43), `_package_sync_test_config`, `_write_package_sync_config`, the automation-env helpers, and `test_all_managers_diff_before_any_applies` (~line 1089); these encode the coordinator assumption and the old module paths.
    - src/pcswitcher/jobs/packages/state.py and packages/review.py and packages/items.py — the new import paths the suite must use (post plan 02-19 move).
    - src/pcswitcher/jobs/manual_installs_sync.py — the job's `name` and `manager_id`, so the config helper and item-id construction match.
    - .planning/phases/02-package-management-sync/02-15-SUMMARY.md and 02-17-SUMMARY.md and 02-18-SUMMARY.md — what the per-manager review, the new job and the snippet push actually shipped as, so the tests assert the real shape.
  </read_first>
  <action>
Update the imports in `tests/integration/jobs/test_package_sync.py` from `pcswitcher.jobs.package_items` / `package_review` / `package_state` to `pcswitcher.jobs.packages.items` / `packages.review` / `packages.state`. Update the module docstring: it currently says a missing package travels "the coordinator's one batched review"; rewrite it to describe each manager's own batched review, and drop the "cross-manager batched review ordering" line.

Reframe `test_all_managers_diff_before_any_applies`. With the coordinator gone, "no manager mutates before every manager has diffed" is no longer the contract and its log-scraping witness (`"2 package manager(s) planned"`) no longer exists. Replace it with a test whose contract is the corrected one: with two package jobs enabled and both machines diverged, each enabled manager completes its OWN review before that same manager issues its OWN first mutating command. Keep the test's end-state assertions (both items converged, asserted against pc2's own `apt-mark showmanual` and `snap list`) and its restore-in-finally. For the ordering witness, if a run-log signal is still needed, assert per manager that that manager's review-complete log line precedes its own first converge line — do not assert an inter-manager ordering. Update the test's docstring to state this is now a per-manager property.

Add `manual_installs_sync` to the config helpers where a test enables it: `_package_sync_test_config`/`_write_package_sync_config` already take `**enabled_jobs`, so passing `manual_installs_sync=True` works once the job exists — verify the helper emits it in the `sync_jobs` block.

Sweep the whole module for any remaining reference to a coordinator, to "planned; review covers", or to three-jobs-only assumptions, and correct each to the per-manager, four-job shape. Leave every assertion that already checks the target's own package-manager state unchanged.

Also inspect `tests/integration/test_config_sync.py`: plan 02-18 reverted `config_sync` to `config.yaml` only. If this test asserts multi-file (snippet-registry) transfer via config_sync, correct it to the single-file contract; if it does not touch the registry, leave it. Include it in `files_modified` because the D-23 revert may have made one of its assertions stale.
  </action>
  <verify>
    <automated>! grep -n 'PackagePhaseCoordinator\|coordinate_package_review\|package manager(s) planned\|jobs.package_items\|jobs.package_review\|jobs.package_state' tests/integration/jobs/test_package_sync.py &amp;&amp; uv run ruff check tests/integration/jobs/test_package_sync.py tests/integration/test_config_sync.py &amp;&amp; uv run basedpyright tests/integration/jobs/test_package_sync.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'jobs.package_items\|jobs.package_review\|jobs.package_state' tests/integration/jobs/test_package_sync.py` returns nothing; imports use `jobs.packages.*`.
    - `grep -n 'PackagePhaseCoordinator\|coordinate_package_review\|package manager(s) planned' tests/integration/jobs/test_package_sync.py` returns nothing.
    - The former `test_all_managers_diff_before_any_applies` is replaced by a per-manager review-before-own-mutation test whose docstring states the per-manager property.
    - `_write_package_sync_config(..., manual_installs_sync=True)` emits `manual_installs_sync: true` in the generated config's `sync_jobs` block.
    - `tests/integration/test_config_sync.py` asserts config_sync's single-file `config.yaml` contract and no multi-file snippet transfer.
    - `uv run ruff check` and `uv run basedpyright` are clean on the changed integration files (they collect without VM access).
  </acceptance_criteria>
  <done>The integration suite imports the moved modules and encodes the per-manager, four-job design, with no coordinator assumption anywhere.</done>
  <reversibility rating="reversible">Test-file edits, reverted in git; no machine state persists between CI runs (each test restores pc2).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add apt-repository-state and manual-installs VM coverage</name>
  <read_first>
    - tests/integration/jobs/test_package_sync.py — the fixtures (`pc1_executor`, `pc2_executor`, `pc1_with_pcswitcher_mod`, `pc2_with_pcswitcher`, `reset_pcswitcher_state`), the candidate-selection and restore helpers, and the automation-env prefix helpers; reuse them.
    - src/pcswitcher/jobs/apt_sync.py — the `/etc/apt` capture/diff/converge path (`_plan_repo_diffs`, `_diff_apt_sources`, `_diff_apt_keys`, `_ensure_repo_group_converged`, `_metadata_refresh_diff`), so the test sets up a divergence the job will actually detect and the review entries match the real item labels.
    - src/pcswitcher/jobs/manual_installs_sync.py and packages/state.py — `SnippetRegistry`, `SNIPPET_REGISTRY_RELPATH`, and how a snippet is registered, so the test seeds a source snippet and asserts target replay.
    - tests/integration/conftest.py — the VM fixtures and btrfs-reset mechanics, to know what a test may safely mutate and how teardown restores it.
    - .planning/WINDOWS.md — entry #2's exact wording, so the new test satisfies it literally (missing vendor repo, key and source as separate review entries, intended apt-get update reported).
  </read_first>
  <action>
Add a VM-level apt-repository-state test. Set up a target (pc2) that is missing one vendor repository the source (pc1) has — remove one file from pc2's `/etc/apt/sources.list.d` and its paired keyring from `/etc/apt/keyrings`, chosen from a repo present on pc1, saving enough to restore both in a `finally`. Run `pc-switcher sync pc2 --dry-run` from pc1 with `apt_sync` enabled, driving the review non-interactively via `PCSWITCHER_PACKAGE_REVIEW_AUTOMATION`. Assert, against the run's own review output (this is the one place a log witness is legitimate, because a dry-run makes no filesystem change to assert against): the missing source file appears as its own review entry, its signing key appears as a separate review entry, and the intended `apt-get update` (the metadata-refresh marker) is reported. This is exactly ledger entry #2's contract. Because it is `--dry-run`, it changes nothing on pc2; still restore in a `finally` for safety against a partial run. Optionally add a non-dry-run companion that actually converges the repo group and asserts pc2's `/etc/apt/sources.list.d` and `/etc/apt/keyrings` gained the files (asserted by `sudo test -f`/`sha256sum` on pc2, target-state not log) and that `apt-get update` succeeded — restoring pc2's `/etc/apt` in a `finally`. Keep the dry-run test as the ledger-closing one; the converge companion strengthens it if VM time allows.

Add a `manual_installs_sync` VM test. Seed a snippet on pc1's source registry for an unreproducible item (a small, safe, idempotent install — e.g. dropping a marker file under `/usr/local/bin` via the snippet, so the assertion is filesystem state, not a package). Run a sync from pc1 to pc2 with `manual_installs_sync` enabled, answering the review non-interactively to apply/replay the snippet. Assert the snippet registry reached pc2 (the file exists at `~/.config/pc-switcher/package-snippets.yaml` on pc2) and that the snippet's effect is present on pc2 (the marker file exists), proving the job's own `send_file()` push plus replay. Restore pc2 (remove the marker file and the pushed registry) in a `finally`.

Every new test asserts target state (pc2's `/etc/apt`, its filesystem, its registry file) rather than pc-switcher log text, except the dry-run repo test whose subject is the review output itself. Follow the module's existing candidate-selection-then-restore discipline and `pytest.skip` with a clear message when the VM lacks a usable repo/snap to diverge.

Update `.planning/phases/02-package-management-sync/02-VALIDATION.md`: add rows for the new apt-repository-state and manual-installs integration tests with their automated commands (`tests/run-integration-tests.sh tests/integration/jobs/test_package_sync.py`), and update the coordinator/three-job rows to the per-manager/four-job reality. Leave `nyquist_compliant` reflecting the truth: the integration rows are pending until CI runs them (no local VM access).
  </action>
  <verify>
    <automated>uv run ruff check tests/integration/jobs/test_package_sync.py &amp;&amp; uv run basedpyright tests/integration/jobs/test_package_sync.py &amp;&amp; grep -q 'sources.list.d' tests/integration/jobs/test_package_sync.py &amp;&amp; grep -q 'keyrings' tests/integration/jobs/test_package_sync.py &amp;&amp; grep -qi 'manual_installs_sync' tests/integration/jobs/test_package_sync.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/jobs/test_package_sync.py` contains a dry-run test that diverges pc2's `/etc/apt` by one vendor repo and asserts the source file and its key appear as separate review entries and the intended `apt-get update` is reported.
    - The same file contains a `manual_installs_sync` test asserting the pushed registry file exists on pc2 and the snippet's filesystem effect is present on pc2.
    - `grep -q 'sources.list.d' tests/integration/jobs/test_package_sync.py` and `grep -q 'keyrings' ...` both succeed — apt repository state is now exercised at VM level.
    - Every new test restores pc2's mutated state in a `finally`.
    - `02-VALIDATION.md` has rows for the two new integration tests and no longer describes a coordinator or three-jobs-only design.
    - `uv run ruff check` and `uv run basedpyright` are clean on the file (collection without VM access).
  </acceptance_criteria>
  <done>The VM suite now exercises apt repository state and the manual-installs snippet push, satisfying ledger entry #2's literal contract, pending its green CI run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the reworked suite passes in CI and close ledger entry #2</name>
  <what-built>The reworked VM integration suite: retargeted to the new modules and the per-manager review, with new apt-repository-state and manual_installs_sync coverage that closes broken-window ledger entry #2. No local VM access exists, so the real proof is a CI run on PR #206 with draft=false (a full VM run takes ~21 minutes).</what-built>
  <how-to-verify>
Mark PR #206 (branch `gsd/phase-02-package-management-sync`) ready-for-review so the `integration-tests` workflow runs, or push the delta commits to it. Confirm in the GitHub Actions run for that PR that `tests/integration/jobs/test_package_sync.py` passes, including the new apt-repository-state dry-run test and the `manual_installs_sync` test, and that the whole package-sync integration module is green (prior baseline was 60 passed, 5 skipped). Once green, run `gsd-tools windows fixed 2` to close broken-window ledger entry #2, and confirm `.planning/WINDOWS.md` shows `open_count: 0`.
  </how-to-verify>
  <resume-signal>Type "approved" once CI is green on PR #206 and ledger entry #2 is marked fixed, or describe the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test log vs target state | An integration test that asserts against pc-switcher's log instead of the target's package manager can pass while the real machine is wrong |
| VM mutation vs teardown | A test that mutates pc2's /etc/apt or package state and fails to restore leaves the shared VM dirty for the next test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-54 | Spoofing | integration assertions | high | mitigate | Every new test asserts pc2's own `/etc/apt`, filesystem or registry state, never a log line — except the dry-run repo test whose subject is legitimately the review output |
| T-02-55 | Tampering | pc2 /etc/apt and package state | high | mitigate | Every mutation (removed repo file, removed key, seeded marker, pushed registry) is restored in a `finally`, following the module's existing restore discipline |
| T-02-56 | Repudiation | broken-window ledger #2 | high | mitigate | The entry is marked fixed only after the CI run that exercises the new coverage is green, gated by the human checkpoint |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
Confirm the reworked module imports `jobs.packages.*`, names no coordinator, and greps positive for `sources.list.d`, `keyrings` and `manual_installs_sync`. The definitive verification is the CI run on PR #206 with draft=false; the human checkpoint gates marking ledger entry #2 fixed on that green run.
</verification>

<success_criteria>
The integration suite reflects the corrected architecture and module paths; apt repository state and the manual-installs snippet push are exercised at VM level; ledger entry #2 is closed once CI is green.
</success_criteria>

<output>
Create `.planning/phases/02-package-management-sync/02-21-SUMMARY.md` when done.
</output>
</content>
