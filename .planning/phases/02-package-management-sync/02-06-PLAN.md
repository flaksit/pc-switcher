---
phase: 02-package-management-sync
plan: 06
type: execute
wave: 4
depends_on: ["02-05"]
files_modified:
  - src/pcswitcher/jobs/package_items.py
  - src/pcswitcher/jobs/apt_sync.py
  - tests/unit/jobs/test_apt_sync.py
autonomous: true
requirements: [REQ-sync-scope-packages, REQ-conflict-detection-no-resolution]

must_haves:
  truths:
    - "apt sources (`/etc/apt/sources.list.d`), per-repo keyrings (`/etc/apt/keyrings`), legacy global-trust keys (`/etc/apt/trusted.gpg.d`), pins (`/etc/apt/preferences.d`) and apt config (`/etc/apt/apt.conf.d`) are captured, diffed and converged as items with the same three-way decision as packages (D-11, D-13)."
    - "A repository's signing key travels bundled with its repository item and is copied byte-for-byte; no key is fetched from a vendor over the network during a sync (D-12)."
    - "Legacy `/etc/apt/trusted.gpg.d` keys, which no repository references explicitly, form their own global-trust item set distinct from per-repo keyrings (D-12)."
    - "A repository item's original file format is preserved on transfer — a deb822 `.sources` file arrives as `.sources` and a legacy `.list` file arrives as `.list`; neither is normalised into the other."
    - "Convergence order for a new repository is key first, then source file, then `apt-get update`, so the first install that needs it does not fail signature verification."
    - "Every repository and key item appears in the review as its own named entry before it is written to the target."
    - statement: "Repository item identity is the filename under `sources.list.d`, so two files describing the same URI are two items rather than one, and the conflicting-values failure mode that arises when both a `.list` and a `.sources` file define one repo is visible as a duplicate rather than silently merged."
      verification: backstop
  artifacts:
    - src/pcswitcher/jobs/apt_sync.py
    - tests/unit/jobs/test_apt_sync.py
  key_links:
    - "`AptSyncJob.collect_hold_pin_facts` reads `preferences.d` content that is also captured as pin items, so the pin's role as both a diff fact and a synced item stays consistent."
    - "Keys are transferred with `RemoteExecutor.send_file` on the raw bytes; apt's own verification on `apt-get update`/`install` is what actually checks the signature."
  prohibitions:
    - statement: "A repository or signing key MUST NOT be added to the target without appearing as its own named review item first; silently expanding a machine's trust set is the failure this item model exists to prevent."
    - statement: "A signing key MUST NOT be re-fetched from a vendor URL, regenerated, or modified in transit; only the source machine's own key bytes are transferred (D-12)."
    - statement: "`/etc/apt` MUST NOT be mirrored with deletion; the target's own machine-specific repositories survive a sync unless their removal was individually reviewed (D-11)."
---

<objective>
Expand `apt_sync` from packages to the repository state those packages come from: sources, keys, pins and apt config, each as an item with the same three-way decision.

Purpose: replicating a package list without its repositories produces a target where half the installs fail with "no candidate". And doing it as a file mirror would wipe the target's own machine-specific sources — which is exactly why D-11 makes them items instead. The bundled-key rule (D-12) matters for the same reason: a repo that arrives without its key is a repo apt will refuse.

Output: five new apt item classes, captured, diffed, reviewed and converged in the order apt actually requires.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-management-sync/02-CONTEXT.md
@.planning/phases/02-package-management-sync/02-RESEARCH.md
@docs/adr/adr-020-declarative-package-convergence.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository, key, pin and apt-config item classes with capture and diff</name>
  <files>src/pcswitcher/jobs/package_items.py, src/pcswitcher/jobs/apt_sync.py, tests/unit/jobs/test_apt_sync.py</files>
  <read_first>
    - src/pcswitcher/jobs/package_items.py — `ItemClass`, `ItemDiff`, `AptPackageItem`'s `item_id`/`label` convention, `build_detail`
    - src/pcswitcher/jobs/apt_sync.py — the existing `capture_source_items` / `query_target_items` / `converge` implementations to extend
    - src/pcswitcher/jobs/package_sync_core.py — the per-item-class dispatch in `diff_items` and the `collect_hold_pin_facts` hook
    - .planning/phases/02-package-management-sync/02-RESEARCH.md — the Standard Stack rows for `/etc/apt/*`, Pitfall 3 on `.list`/`.sources` coexistence, and the State of the Art table on apt-key deprecation
    - .planning/phases/02-package-management-sync/02-CONTEXT.md — D-11, D-12, D-13, and the P17 inventory numbers in the specifics block
  </read_first>
  <behavior>
    - Capture returns one `AptSourceItem` per file in `/etc/apt/sources.list.d`, with the filename as identity and the file's SHA-256 as the comparison basis; both a `.list` and a `.sources` file yield items and the format is recorded on the item.
    - Capture returns one `AptKeyItem` per file in `/etc/apt/keyrings`, each linked to the source item(s) whose content references its path.
    - Capture returns one `AptKeyItem` per file in `/etc/apt/trusted.gpg.d`, marked as global-trust and linked to no source item.
    - Capture returns one `AptPinItem` per file in `/etc/apt/preferences.d` and one `AptConfigItem` per file in `/etc/apt/apt.conf.d`.
    - A file present on the source and absent on the target yields `MISSING_ON_TARGET` with the appropriate additive action for its class.
    - A file present on both with differing digests yields `VERSION_MISMATCH` with action `CHANGE` and a detail naming both digests.
    - A file present on the target and absent on the source yields `EXTRA_ON_TARGET` with action `REMOVE`.
    - A source item whose referenced keyring file is absent on the source yields a diff whose detail flags the dangling key reference, and the source item is not proposed for install on its own.
    - `collect_hold_pin_facts` returns one `HoldPinFact` per package named in a captured `preferences.d` file, with `mechanism="pin"` and the filename as `source_ref`.
  </behavior>
  <action>
Add four frozen item dataclasses to `package_items.py`, each with `item_id` and `label()` following `AptPackageItem`'s convention and each identified by its filename, not by its parsed content — RESEARCH Pitfall 3 shows that both file formats can legitimately coexist in `sources.list.d`, so filename identity is what keeps a double-defined repo visible as two items rather than being silently merged into one:

- `AptSourceItem`: `filename: str`, `digest: str`, `fmt: Literal["deb822", "list"]`, `keyring_refs: tuple[str, ...]`. Identity `apt:source:<filename>`.
- `AptKeyItem`: `filename: str`, `digest: str`, `scope: Literal["per-repo", "global-trust"]`. Identity `apt:key:<scope>:<filename>`.
- `AptPinItem`: `filename: str`, `digest: str`, `pinned_packages: tuple[str, ...]`. Identity `apt:pin:<filename>`.
- `AptConfigItem`: `filename: str`, `digest: str`. Identity `apt:config:<filename>`.

Add a module-level docstring paragraph explaining the filename-identity choice and the two coexisting source formats.

Extend `AptSyncJob.capture_source_items` and `query_target_items` to gather all five classes in addition to packages. Read the five directories with one `sudo find <dir> -maxdepth 1 -type f -exec sha256sum {} +` invocation per directory (quoted), so a digest comparison needs no file transfer, and fetch the full content only for the files a diff actually implicates. Parse just enough of each source file to extract its keyring references — `Signed-By:` for deb822 and `signed-by=` inside the options brackets for legacy `.list` — and record which format it was so the transfer can preserve it. Do not normalise, rewrite or migrate either format; the deferred-ideas list is explicit that migrating legacy trust keys is its own piece of work.

Parse `preferences.d` files only far enough to list the package names each stanza names, so `collect_hold_pin_facts` can emit `mechanism="pin"` facts. Diff the file itself by whole-file digest rather than by parsed stanza — RESEARCH's alternatives table recommends whole-file for v1 and CONTEXT leaves the choice to discretion; record that choice in a comment so a later reader knows it was decided, not overlooked.

Extend the `diff_items` dispatch in the base with the four new classes. Add the dangling-keyring detail: when a source item names a keyring path that produced no `AptKeyItem`, the source item's diff carries that fact and the converge stage must not add the repo alone.

Extend `tests/unit/jobs/test_apt_sync.py` with the behavior bullets, using stub executors returning realistic `sha256sum` output and realistic deb822 and `.list` file bodies (a `Signed-By:` deb822 entry and a `[signed-by=…]` legacy entry, both drawn from the shapes RESEARCH verified on the reference machine).
  </action>
  <acceptance_criteria>
    - `src/pcswitcher/jobs/package_items.py` defines `AptSourceItem`, `AptKeyItem`, `AptPinItem` and `AptConfigItem`.
    - `AptKeyItem` carries a `scope` field distinguishing per-repo keyrings from global-trust keys.
    - A unit test asserts a deb822 file and a legacy file both produce items and that each item records its own format.
    - A unit test asserts a keyring referenced by a source file is linked to it, and that a global-trust key is linked to no source.
    - A unit test asserts a source item whose keyring reference resolves to nothing carries the dangling-reference detail.
    - A unit test asserts `collect_hold_pin_facts` emits a pin fact per package named in a captured preferences file.
    - `uv run pytest tests/unit/jobs/test_apt_sync.py -x` exits 0.
    - `uv run ruff check . && uv run basedpyright` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/jobs/test_apt_sync.py -x &amp;&amp; uv run ruff check . &amp;&amp; uv run basedpyright</automated>
  </verify>
  <done>All five apt item classes are captured on both machines and diffed by whole-file digest, with keyring references resolved and pin facts extracted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ordered convergence of repository state on the target</name>
  <files>src/pcswitcher/jobs/apt_sync.py, tests/unit/jobs/test_apt_sync.py</files>
  <read_first>
    - src/pcswitcher/jobs/apt_sync.py — the `converge` dispatch extended in plan 02-05 with `REMOVE`/`CHANGE`
    - src/pcswitcher/jobs/vscode_state_sync.py — `_sync_editor`: the `send_file` to a sibling temp path followed by an atomic `mv -f`, the transfer shape this reuses
    - src/pcswitcher/executor.py — `RemoteExecutor.send_file`, and the fact that the target has no direct filesystem access from here
    - src/pcswitcher/jobs/package_sync_core.py — the per-item continue-on-error handling and the converge loop ordering
    - .planning/phases/02-package-management-sync/02-CONTEXT.md — D-12, D-14, D-27
  </read_first>
  <behavior>
    - Converging a new repository whose key is also new issues the key write before the source write, and issues exactly one `apt-get update` after both, before any package install in the same run.
    - Converging a repository whose key already matches on the target issues no key write.
    - A key or source file is written by transferring the source's raw bytes to a temp path in the destination directory and moving it into place — the file's content on the target is byte-identical to the source's.
    - A key is never fetched over the network: no converge command for a key item contains a URL.
    - Converging a `REMOVE` on a source item deletes that file and issues an `apt-get update` afterwards, and does not delete any other file under `/etc/apt`.
    - `apt-get update` runs at most once per job execution even when several repository items converged.
    - With `dry_run=True`, no key write, no source write, no delete and no `apt-get update` reaches the target.
    - A failed key write is collected as a per-item failure and the dependent source item for that key is not written.
  </behavior>
  <action>
Extend `AptSyncJob.converge` to handle the four repository item classes, and add an ordering stage to the job's execute path.

Ordering: the base converge loop is item-by-item, but apt has a hard dependency chain — a repository whose key is missing fails verification, and a package from a repository whose metadata was never fetched has no candidate. Rather than making the base loop dependency-aware, have `AptSyncJob` sort its approved diffs before handing them to the loop: keys first, then pins and apt config, then sources, then a single `apt-get update` marker item, then packages. Document that this ordering is an apt fact rather than a general one, which is why it lives in the job and not in the shared core.

Represent the metadata refresh as a synthetic diff the job inserts when at least one source, key or pin item converged, so it flows through the same per-item logging, dry-run gate and failure collection as everything else instead of being a special case bolted onto the end. Insert it at most once.

File writes: read the source file's bytes locally, `send_file` them to `<destination>.pcswitcher-tmp` in the destination directory, then `sudo mv -f` into place and `sudo chown root:root` plus `sudo chmod 644` (0644 for sources, pins and apt config; keys under `keyrings/` and `trusted.gpg.d/` also 0644 — apt requires them world-readable). Preserve the original filename and therefore the original format extension. Use `shlex.quote` on every path.

Key/source coupling: before writing a source item, check that every keyring path it references either already matches on the target or is itself among the approved-and-successfully-converged key items in this run. If not, skip the source write, log at ERROR naming the missing keyring, and collect it as a per-item failure (D-27) — a repository written without its key is a repository apt will refuse on every subsequent operation, so writing it anyway makes the target worse than leaving it alone.

Removals: delete the single named file with `sudo rm -f <quoted path>`. Never use a recursive or glob delete, and never run rsync with `--delete` against `/etc/apt` — the target's own machine-specific repositories must survive (D-11).

Extend `tests/unit/jobs/test_apt_sync.py` with the behavior bullets, asserting on the exact ordered command sequence the stub target executor received.
  </action>
  <acceptance_criteria>
    - A unit test asserts the recorded command order for a new-repo-plus-new-key scenario is key write, then source write, then `apt-get update`, then package install.
    - A unit test asserts `apt-get update` appears exactly once when three repository items converge.
    - A unit test asserts no converge command for an `AptKeyItem` contains `http://` or `https://`.
    - A unit test asserts a failed key write leaves the dependent source item unwritten and records one failure.
    - A unit test asserts a `REMOVE` on a source item issues exactly one `rm -f` naming that file and no other delete.
    - A unit test asserts `dry_run=True` produces zero mutating commands across all four repository classes.
    - `uv run pytest` (full unit suite) exits 0.
    - `uv run ruff check . && uv run ruff format --check . && uv run basedpyright` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/jobs/test_apt_sync.py -x &amp;&amp; uv run pytest &amp;&amp; uv run ruff check . &amp;&amp; uv run basedpyright</automated>
  </verify>
  <done>Repository state converges in the order apt requires, keys travel as verbatim bytes bundled with their repositories, and nothing under `/etc/apt` is deleted except an individually-reviewed file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source `/etc/apt` → target `/etc/apt` | Repository definitions and signing keys cross machines and expand what the target will trust and install from. |
| transferred keyring bytes → apt's verification | The key the target uses to verify every future package from that repository. |
| `/etc/apt` file paths → root shell commands | Filenames from a directory listing are interpolated into root-privileged write and delete commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Spoofing | signing-key provisioning | high | mitigate | Keys are copied byte-for-byte from the source's own verified keyring; no converge command contains a vendor URL and no key is regenerated or re-derived. apt's own verification on `update`/`install` performs the actual cryptography (D-12, ASVS V6). |
| T-02-04 | Tampering | trust-set expansion | high | mitigate | Every source and key item is its own named review entry with the three-way decision; adding a repository to the target is always a decision the user saw, never a side effect of installing a package (D-11). |
| T-02-15 | Tampering | `/etc/apt` deletion | high | mitigate | Removals delete one named file via `rm -f`; there is no recursive delete and no rsync `--delete` against `/etc/apt`, so the target's machine-specific repositories survive (D-11). |
| T-02-16 | Denial of Service | repo written without its key | medium | mitigate | A source item whose keyring is missing or whose key write failed is skipped and reported, rather than written to produce a target where every apt operation fails signature verification. |
| T-02-01 | Tampering | filename interpolation into root commands | high | mitigate | `shlex.quote()` on every path in every write, move, chown, chmod and delete command (ASVS V5). |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `uv run pytest` green.
- `uv run ruff check . && uv run ruff format --check . && uv run basedpyright` green.
- A dry-run against a target missing one vendor repository shows the key item and the source item as separate review entries and reports the intended `apt-get update`.
</verification>

<success_criteria>
apt repository state replicates as reviewable items in the order apt requires, keys travel bundled and verbatim with their repositories, and no file under `/etc/apt` is written or deleted without having appeared in the review.
</success_criteria>

## Artifacts this phase produces

Created by this plan:

- Symbols: `AptSourceItem`, `AptKeyItem`, `AptPinItem`, `AptConfigItem`, and the `AptSyncJob` converge-ordering stage plus its synthetic metadata-refresh diff

Created elsewhere in Phase 2: see the same section in `02-01-PLAN.md` for the full phase-wide symbol list.

<output>
Create `.planning/phases/02-package-management-sync/02-06-SUMMARY.md` when done.
</output>
