---
phase: 02-package-management-sync
plan: 20
type: execute
wave: 6
depends_on:
  - 02-19
files_modified:
  - docs/configuration.md
  - docs/jobs/package-sync.md
  - docs/jobs/folder-sync.md
  - docs/jobs/vscode-state-sync.md
  - docs/README.md
  - docs/system/architecture.md
  - docs/system/core.md
  - docs/system/data-model.md
autonomous: true
requirements:
  - REQ-sync-scope-packages
  - REQ-conflict-detection-no-resolution
must_haves:
  truths:
    - "docs/configuration.md explains configuration only; the description of what each job does has moved into per-job documents."
    - "The package jobs are documented together in one per-job document covering the item -> diff -> review -> converge model, the four jobs, the per-manager review, machine-specific packages, install snippets, and versions."
    - "No document states that one review spans every enabled package manager, that a PackagePhaseCoordinator exists, or that resolving an unreproducible item is mandatory; every doc reflects the corrected per-manager review, four jobs, skip-once-is-valid, and self-pushed snippets."
    - "The living specs in docs/system/ reference the new jobs/packages/ module paths and the manual_installs_sync job, and describe each package job reviewing and applying inside its own execute()."
    - "docs/system/data-model.md describes the snippet registry travelling by manual_installs_sync's own send_file() push after its review, not via config_sync."
    - statement: "Every job's behavioural documentation lives in a per-job document, including the pre-phase jobs whose explanations move out of configuration.md."
      verification: backstop
  prohibitions:
    - "No document describes a cross-manager review coordinator or a single review across all managers under any wording."
    - "The configuration reference never re-acquires prose describing what a job does beyond naming its config keys."
    - "No document claims the snippet registry travels via config_sync or SYNCED_CONFIG_FILENAMES."
  artifacts:
    - docs/jobs/package-sync.md
    - docs/configuration.md
    - docs/system/architecture.md
  key_links:
    - "The corrected decisions in ADR-020 (plan 02-14) are the source of truth; every doc statement must agree with the rewritten ADR, not with the shipped-but-superseded coordinator design."
    - "docs/system/ living specs are the authority per ADR-011/ADR-012; a stale coordinator diagram or an obsolete module path there misleads the next planner exactly as a stale ADR would."
---

<objective>
Migrate job behaviour documentation out of the configuration reference into per-job documents (D-33), and remove every trace of the superseded coordinator, one-cross-manager-review, mandatory-registration and config_sync-snippet-transport claims from the docs.

Purpose: `configuration.md` and `default-config.yaml` explain configuration; what a job does belongs in the job's own document. This applies to the jobs that predate this phase too — their explanations move out. The package jobs share the item -> diff -> review -> converge model closely enough to be documented together. Separately, the living specs and the configuration reference still describe `PackagePhaseCoordinator`, one batched review across managers, mandatory unreproducible resolution, and the registry riding `config_sync` — all corrected by this delta and all now wrong.

Output: a `docs/jobs/` set with a combined package-sync document and per-job documents for the pre-phase jobs; `configuration.md` restricted to configuration; and `docs/system/` living specs corrected to the per-manager, four-job, self-pushed-snippet design against the new module paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-management-sync/02-CONTEXT.md
@docs/adr/adr-020-declarative-package-convergence.md
@docs/configuration.md
</context>

<artifacts_this_phase_produces>
Created by this plan:

- `docs/jobs/package-sync.md` — the combined behavioural document for `apt_sync`, `snap_sync`, `flatpak_sync` and `manual_installs_sync`.
- `docs/jobs/folder-sync.md` — behavioural document for `folder_sync` (moved out of configuration.md's folder_sync prose).
- `docs/jobs/vscode-state-sync.md` — behavioural document for `vscode_state_sync` (moved out of configuration.md).

If the project already has an established per-job doc location or index, use it instead of inventing `docs/jobs/`; the executor confirms against `docs/README.md` and `docs/system/_index.md` before choosing the path, and this plan's `files_modified` is adjusted to match. No code symbols are created.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Split job behaviour out of configuration.md into per-job docs</name>
  <read_first>
    - docs/configuration.md — read in full; its `## folder_sync`, `## vscode_state_sync` and `## Package Sync` sections mix configuration with behaviour, and the behaviour parts move out.
    - docs/README.md and docs/system/_index.md — to confirm whether a per-job doc location already exists and how docs cross-link, before creating `docs/jobs/`.
    - docs/adr/adr-020-declarative-package-convergence.md — the rewritten ADR (plan 02-14); the package-sync doc must agree with it on the per-manager review, four jobs, skip-once, and snippet transport.
    - .planning/phases/02-package-management-sync/02-CONTEXT.md — D-33 (documentation layout) and the corrected D-15/D-21/D-23/D-24/D-30 the doc must reflect.
  </read_first>
  <action>
Create `docs/jobs/package-sync.md` and move into it the behavioural content currently under `## Package Sync` in `configuration.md` — "What each job covers", "The batched review", "Machine-specific packages", "Install snippets", "Versions", "Non-interactive runs" and the passwordless-sudo prerequisites — rewritten to the corrected decisions:

- Four jobs, not three: add `manual_installs_sync` and describe it owning unreproducible detection (apt-no-candidate packages plus unowned `/usr/local` and `/opt` files) and the install-snippet registry, with its own enable flag independent of `apt_sync`.
- Per-manager review: each enabled package job shows its own batched review before it applies anything; there is no single review spanning every manager and no coordinator. Delete the paragraph beginning "Before anything is changed on any manager, pc-switcher shows one review covering every enabled package job at once".
- Skip-once is a valid resolution: an unreproducible item ends a run resolved by a snippet, a machine-specific mark, or a deliberate skip-once; only a genuinely undecided item makes the run unclean. Delete the "Resolving an unreproducible item is mandatory" paragraph and replace it with the corrected three-way framing.
- apt collateral (D-30): the review shows collateral apt would cause; auto-installed dependency collateral proceeds, while a manually-installed package the transaction would remove or downgrade becomes its own reviewable item offering install-anyway, skip or abort — decided in the review, never mid-apply.
- Snippet transport (D-23): the registry (`~/.config/pc-switcher/package-snippets.yaml`) is pushed to the target by `manual_installs_sync` itself right after its review, so an on-the-fly snippet reaches the target the same run. Delete the paragraph saying it "travels alongside config.yaml during config sync".

Create `docs/jobs/folder-sync.md` and `docs/jobs/vscode-state-sync.md` and move the behavioural prose for those jobs out of `configuration.md` into them (the filter-rule mechanics, the authorized_keys guidance, the always-excluded groups, the VS Code selective-merge explanation). Keep behaviour explanations there; leave only configuration in `configuration.md`.

Rewrite `configuration.md` so each job section states only its configuration: the config keys, their types, defaults and validation, plus a one-line pointer to the job's document for what it does. For the package jobs, that is just the `sync_jobs` enable flags and a link to `docs/jobs/package-sync.md`; there are no per-job config keys (D-32). For `folder_sync`, keep the `folders`/`filter_file` key reference and link out for the filter-rule semantics. Preserve the `logging`, `disk_space_monitor`, `btrfs_snapshots` and `dummy_*` sections as configuration reference; if they carry behavioural prose, trim it to configuration and link to a job doc where one exists, but do not invent job docs for the test/dummy jobs.

Cross-link: add the three new per-job docs to `docs/README.md`, the user-facing documentation index (it already lists `configuration.md` and `reading-sync-logs.md` under "For Users"); add a "Job behaviour (`jobs/`)" group or fold the entries into the existing "For Users" list, matching that file's existing link style. Do not edit `docs/system/_index.md` for this — the per-job docs are user-facing behaviour docs, not living specs, and `_index.md` indexes only the `system/` golden-copy specs (task 2 edits the `system/` docs' content but adds no new `system/` file, so `_index.md` needs no change). Use Mermaid for any diagram, no `---` separators, no bold-as-heading, no mid-sentence hard wraps (project markdown rules).
  </action>
  <verify>
    <automated>test -e docs/jobs/package-sync.md &amp;&amp; grep -qi 'manual_installs_sync' docs/jobs/package-sync.md &amp;&amp; ! grep -qi 'coordinator\|one review covering every\|mandatory' docs/jobs/package-sync.md &amp;&amp; ! grep -qi 'travels alongside config.yaml\|SYNCED_CONFIG_FILENAMES' docs/jobs/package-sync.md docs/configuration.md &amp;&amp; echo DOCS_SPLIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/jobs/package-sync.md` exists, names all four package jobs including `manual_installs_sync`, and describes a per-manager review.
    - `grep -ci 'coordinator' docs/jobs/package-sync.md docs/configuration.md` returns 0.
    - No sentence in `docs/jobs/package-sync.md` or `docs/configuration.md` says one review covers every manager, calls unreproducible resolution mandatory, or says the registry travels via config_sync.
    - `docs/configuration.md`'s package-sync section contains only the `sync_jobs` enable flags and a link to the job doc, with no per-job config section.
    - `docs/jobs/folder-sync.md` and `docs/jobs/vscode-state-sync.md` exist and hold the behavioural prose moved out of configuration.md.
    - The three new docs are each linked from `docs/README.md`, and contain no `---` separator, no bold-as-heading, and no mid-sentence hard wrap.
  </acceptance_criteria>
  <done>Configuration lives in the configuration reference; job behaviour lives in per-job docs; the package doc reflects the corrected decisions.</done>
  <reversibility rating="reversible">Documentation only; a git revert restores the prior docs.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Correct the docs/system living specs</name>
  <read_first>
    - docs/system/architecture.md — the `## Package Sync Subsystem` section, the `Plan → review → apply, and why a coordinator owns the middle step` subsection, the Mermaid `Coordinator` subgraph, and the step-9 sync-sequence line naming the coordinator.
    - docs/system/core.md — the package-sync paragraphs naming `package_sync_core.py` and asserting a coordinator-supplied accepted plan.
    - docs/system/data-model.md — the snippet-registry entry (~line 217) saying it travels via `config_sync`'s `SYNCED_CONFIG_FILENAMES`.
    - docs/adr/adr-020-declarative-package-convergence.md — the corrected authority the specs must match.
    - .planning/phases/02-package-management-sync/02-CONTEXT.md — the corrected decisions.
  </read_first>
  <action>
Correct the living specs to the delivered post-delta design.

In `docs/system/architecture.md`: rewrite the `## Package Sync Subsystem` section. Remove the `PackagePhaseCoordinator` subsection and its rationale; replace the "why a coordinator owns the middle step" prose with a description of each package job running plan -> review -> apply inside its own `execute()`, its review batched per manager and grouped by action, applying nothing until its own review returns. Replace the Mermaid `Coordinator` subgraph with a diagram of one job's plan/review/apply pipeline (Mermaid syntax). Fix the step-9 sync-sequence line so it no longer says the coordinator runs first; each enabled package job runs in the job loop and reviews itself. Add `manual_installs_sync` as the fourth package job and note it pushes its snippet registry to the target itself after its review. Update every `src/pcswitcher/jobs/package_*.py` path reference to the new `src/pcswitcher/jobs/packages/*.py` paths (and note the four job modules stay in `jobs/`).

In `docs/system/core.md`: update the package-sync paragraphs. Point the module reference at `src/pcswitcher/jobs/packages/sync_core.py`. Replace "a job's execute() refuses to run without a coordinator-supplied accepted plan" with the corrected structural property: a job's `execute()` runs its own plan, review and apply in order, applying nothing before its own review returns. Name all four jobs.

In `docs/system/data-model.md`: rewrite the snippet-registry entry so it states the registry is pushed to the target by `manual_installs_sync` with `send_file()` after its own review, and that `config_sync` carries `config.yaml` only. Remove the `SYNCED_CONFIG_FILENAMES` reference. Keep the machine-local decision-file entry's contrast intact (decision files never sync; the registry does reach the target, by the job's own push).

Grep the whole `docs/system/` tree for `coordinator`, `PackagePhaseCoordinator`, `package_sync_core`, `package_items`, `package_review`, `package_state` and `SYNCED_CONFIG_FILENAMES` and confirm none survive except where a corrected sentence legitimately says there is no coordinator. Preserve every unrelated section verbatim.
  </action>
  <verify>
    <automated>! grep -rni 'PackagePhaseCoordinator\|SYNCED_CONFIG_FILENAMES' docs/system/ &amp;&amp; ! grep -rn 'jobs/package_sync_core\|jobs/package_items\|jobs/package_review\|jobs/package_state' docs/system/ &amp;&amp; grep -qi 'manual_installs_sync' docs/system/architecture.md &amp;&amp; echo SPECS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rni 'PackagePhaseCoordinator' docs/system/` returns nothing.
    - `grep -rn 'jobs/package_sync_core\|jobs/package_items\|jobs/package_review\|jobs/package_state' docs/system/` returns nothing; the specs reference `jobs/packages/*` instead.
    - `docs/system/architecture.md` names `manual_installs_sync` and describes each package job reviewing and applying inside its own `execute()`, with a Mermaid diagram that contains no `Coordinator` node.
    - `docs/system/data-model.md` states the snippet registry is pushed by `manual_installs_sync` via `send_file()` and contains no `SYNCED_CONFIG_FILENAMES` reference.
    - `docs/system/core.md` no longer asserts a coordinator-supplied accepted plan and names all four package jobs.
    - No `---` separator or bold-as-heading was introduced, and diagrams are Mermaid.
  </acceptance_criteria>
  <done>The living specs describe the same per-manager, four-job, self-pushed-snippet architecture as the corrected ADR, against the new module paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| living spec to future planner | A stale coordinator diagram or module path in docs/system/ is read as authority and reconstructs removed architecture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-52 | Tampering | docs/system/*.md | high | mitigate | Task 2's grep gates drive coordinator and old-module-path references to zero across the living specs, so the authority docs cannot disagree with the corrected ADR |
| T-02-53 | Repudiation | docs/configuration.md | low | mitigate | Job behaviour moves to per-job docs, so the configuration reference cannot drift into describing behaviour that later changes |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
Grep `docs/` for `PackagePhaseCoordinator`, `SYNCED_CONFIG_FILENAMES`, `jobs/package_sync_core` and "one review covering every"; confirm none survive outside a corrected negation. Read `docs/jobs/package-sync.md` and confirm it matches the rewritten ADR-020 on the four jobs, per-manager review, skip-once, collateral classification and snippet push.
</verification>

<success_criteria>
Configuration and behaviour are separated per D-33; every doc reflects the corrected per-manager, four-job, self-pushed-snippet design; no coordinator or stale module path survives in `docs/`.
</success_criteria>

<output>
Create `.planning/phases/02-package-management-sync/02-20-SUMMARY.md` when done.
</output>
</content>
