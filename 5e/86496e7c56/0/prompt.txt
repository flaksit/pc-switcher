Read .planning/phases/02-package-management-sync/02-HANDOVER-conformance.md and
follow it. Steps 1 and 2 are done — 02-WORK-UNITS.md, 02-DOC-DEBT.md and the
criteria's gap register are their output, and the four rulings step 2 asked for
have been given and are already folded into both.

Do steps 3, 4 and 5, in one go, then stop. Do not do step 6.

Step 5 budget: 02-UAT-01-RUNBOOK.md is 576 lines and nobody will read it. The
rewrite is at most 150, and 02-UAT.md at most 60: per check, the copy-paste
commands and one sentence saying what the tester should see. No reasoning, no
history, no restating the requirement it covers.

---

Will you do everything yourself and fill your context window with details and let auto-compaction set in? Do you deliberately not use subagents? Or do you use them? 
Ensure that you keep your correct clean so they you as orchestrator keep focus on the priorities and what's essential!

---

I meant "keep your CONTEXT clean"

---

<task-notification>
<task-id>a05889a1edce7364a</task-id>
<tool-use-id>toolu_01Sx2n6f11sX6a6ZUqRG1UdH</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/1b0cc20e-9530-4d69-a503-cc392b0e3957/tasks/a05889a1edce7364a.output</output-file>
<status>completed</status>
<summary>Agent "U6+U8 snap sideloads, leaked id" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Both units done.

**U6 — `e2bf330f`** `fix(snap): a sideloaded snap is left alone on both machines`. `SnapSyncJob.plan` now partitions the target listing as well as the source, warns once per machine (`_warn_sideloaded`) naming what it found, and withholds every name sideloaded on *either* machine from *both* item lists. Two cases closed: a target-only sideloaded snap is no longer a removal candidate, and a store snap the target sideloaded under the same name is no longer an install (it would have had to displace the sideloaded copy). Tests: the old `test_target_only_sideloaded_snap_is_still_offered_for_removal` is inverted and now also asserts the warning; a new test covers the source-store/target-sideloaded collision. Docs: `docs/jobs/package-sync.md` § Versions sideload paragraph rewritten; `PKG-FR-SNAP-SIDELOAD` struck from the gap register (its now-empty `### snap` heading removed); DIV-04 marked closed; the U6 doc-debt row deleted.

**U8 — `147f61a5`** `fix(packages): the decision-file header ends at "peer"`. `state._FILE_HEADER` no longer ships `peerfilter_inert`. New `TestDecisionFileRecord::test_header_is_prose_the_user_can_read` asserts the written header line and that no symbol reached the comments. DIV-06 marked closed.

Green before each commit: `ruff format`/`ruff check`/`basedpyright` on the touched files, `codespell` and `pytest tests/unit tests/contract` repo-wide (1550 then 1551 passed). Ran format/lint scoped to my own files rather than `.` so as not to rewrite the other agents' in-flight edits in the shared tree. Nothing left open.</result>
<usage><subagent_tokens>98400</subagent_tokens><tool_uses>41</tool_uses><duration_ms>229370</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aa0a880cb4cb64735</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/1b0cc20e-9530-4d69-a503-cc392b0e3957/tasks/aa0a880cb4cb64735.output</output-file>
<status>completed</status>
<summary>Agent "U3 orchestration conformance" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>U3 done. Commit `db00e959`.

**Changed**
- `orchestrator.py`: `ProbeFailed` now shares the non-aborting arm with `PackageItemFailures` (records FAILED, runs the remaining jobs); that arm's comment no longer claims a cross-manager batched review, it cites D-15/D-16 job independence. `_check_package_jobs_precede_folder_sync` validates `manual_installs_sync` alongside the three managers.
- `probes.py`: `ProbeFailed` docstring now says the *job* fails once and the orchestrator runs the rest.
- Tests: new `tests/unit/orchestrator/test_probe_failure_isolation.py` (4 cases: next job still runs, FAILED result carries the command, session still FAILED); `test_config_system.py` extended to four jobs plus a `manual_installs_sync`-only case.
- Docs: `docs/jobs/package-sync.md` §§ Job ordering / cannot-be-read; `docs/system/package-sync.md` intro + shared-core bullet; criteria gap register — the whole "Reporting and the log" section is gone; DIV-01/05/08 closed in place; the two U3 doc-debt rows deleted.

**Also edited, outside my file list:** `docs/adr/adr-022-…md` D-06 and its Negative bullet asserted "a `ProbeFailed` aborts the entire run, until #220" — false as of this commit. Rewritten to say job-level failure is the ceiling for package reads, with #220 left open for every other job-level exception (folder_sync, vscode_state_sync still terminal). Issue #220 not touched on GitHub.

**Not closed, flagged for whoever owns the file:** `tests/unit/jobs/test_package_sync_core.py::TestOrchestratorPackageItemFailuresContinuation.test_other_exception_types_still_abort_the_run` has a docstring reading "only PackageItemFailures gets the non-aborting branch" — now inaccurate. The test itself still passes and still guards correctly (it raises a plain `RuntimeError`); I left it alone because that file is not mine and another unit is editing it.

**Green:** `ruff format --check .`, `ruff check .`, `basedpyright` (0 errors), `codespell` (clean), `pytest tests/unit tests/contract` — 1551 passed. `src/pcswitcher/jobs/flatpak_sync.py` is left modified in the tree by another agent; not staged.</result>
<usage><subagent_tokens>119583</subagent_tokens><tool_uses>65</tool_uses><duration_ms>338505</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a34f3ff6f1fe46ada</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/1b0cc20e-9530-4d69-a503-cc392b0e3957/tasks/a34f3ff6f1fe46ada.output</output-file>
<status>completed</status>
<summary>Agent "U4 apt repos keys holds" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>U4 is committed as `27c52aad`.

**Measurement (`docker run --rm ubuntu:24.04`)** — the load-bearing finding: `apt-mark hold &lt;pkg&gt;` **exits 0 and records the hold for a package that is merely NOT INSTALLED** (`htop set on hold.`, then `apt-mark showhold` lists it). It exits 100 only for a name apt has never heard of. So `PKG-FR-APT-HOLD-INERT` could not be met by apt's exit code and needed an explicit guard.

What changed:
- `PKG-FR-REPO-DELETE` — `AptProbe.packages_by_source_file` (was `machine_specific_packages_by_source_file`) now counts the target's manual set plus its machine-specific marks over one batched policy call; `AptSyncJob._plan_repo_diffs` subtracts this run's removal candidates and passes the still-in-use filenames to `diff_apt_sources`, which builds no item for them and logs what it kept back. `messages.build_orphaned_packages_detail` deleted; `build_repo_removal_detail` is URLs only.
- `PKG-FR-REPO-CONFLICT` — new `AptSyncJob._files_an_approval_would_write` (union of `OriginPlan.derived_files` over proposed installs) gates the conflict set, mirroring `flatpak_sync._capture_remote_conflicts`.
- `PKG-FR-APT-HOLD-VERSION` — `AptSyncJob._held_versions` feeds `PackageConverger`, which installs `&lt;name&gt;=&lt;version&gt;`; `_held_version_refusal` turns apt's refusal into a failure naming the source's version and the target's candidate (new `commands.candidate_version`), never falling back.
- `PKG-FR-APT-HOLD-INERT` — `PackageConverger._hold_refusal` refuses a hold whose package item was skipped, failed, or only reported the package missing; install outcomes are recorded in `PackageConverger._install_outcome`.
- `PKG-FR-DERIVED-VISIBLE` — `Keyrings.provision`/`remove_unused` log each key at FULL; `AptSyncJob.apply` previews `Keyrings.writes` and the new `Keyrings.unreferenced` under `--dry-run`; `DerivedWrites.written_source_filenames` de-duplicates the computation with `EtcApt`.

Status: `ruff format`/`check`, `basedpyright`, `codespell` clean; `pytest tests/unit tests/contract` 1583 passed. Gap register: four entries struck; one justified survivor left for `PKG-FR-REPO-DELETE` — usage is counted over the target's manual set plus marks, not over automatically-installed packages (counting those needs an `apt-cache policy` over the whole dpkg set on every run that finds a target-only repository). DIV-07 closed; DIV-10 closed for the install case with its unruled half (same package held on both machines at different versions) explicitly left open.

Two notes for the caller: shared files (`docs/jobs/package-sync.md`, `docs/system/package-sync.md`, `tests/unit/test_mutates_audit.py`) were staged hunk-by-hunk so U5's concurrent edits stayed out — one adjacent deletion of theirs was unavoidably included, and they committed cleanly afterwards. `flatpak_sync.py`'s module docstring still references `AptSyncJob._machine_specific_packages_by_source_file`, a name that no longer exists; it is U5's file, so I left it.</result>
<usage><subagent_tokens>328436</subagent_tokens><tool_uses>143</tool_uses><duration_ms>1332820</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>acdd6e8d6de18a511</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/1b0cc20e-9530-4d69-a503-cc392b0e3957/tasks/acdd6e8d6de18a511.output</output-file>
<status>completed</status>
<summary>Agent "U5 flatpak remotes and filter" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>U5 done. Commit `e1e25c69`.

**What changed** (`src/pcswitcher/jobs/flatpak_sync.py`, `tests/unit/jobs/test_flatpak_sync.py`, `docs/jobs/package-sync.md`, `docs/system/{package-sync,data-model}.md`, plus the three shared planning/criteria docs):

- **REMOTE-DELETE**: `_diff_flatpak_remotes` / `_remove_remote_diff` / `build_orphaned_refs_detail` / `_converge_remote` are gone. `_delete_unused_remotes` runs after the converge loop, re-reading the target's remotes and its full ref listing (`flatpak list` without `--app`, so runtimes count) and deleting only what the source lacks and nothing names as its origin. A failed deletion warns; no item is charged. Dry run subtracts approved ref removals so the preview still shows the deletion.
- **FILTER**: capture widened to `name,url,options,filter`. `_apply_remote_filters` stages the file, promotes it with `install` to the same absolute path (privileged by the remote's own scope), and `remote-modify --filter`s it — after the loop. Failure fails every approved ref whose own origin is that remote; `apply()` catches the base `PackageItemFailures` and re-raises one merged exception.
- **REMOTE-TRUST**: `_warn_if_filtered` → `_warn_if_unverified`, a WARNING on any derived remote the source does not verify, dry run included.
- **ORIGIN-DIFF**: `_same_vendor` no longer falls back to names; an absent URL matches nothing. `_origin_display` prints `name (no URL: the machine does not configure name)`.

**Fixture reshape**: the sweep's measured note (`02-SPEC` §2.8) says an unfiltered remote prints `-` in the filter column, so lines are four fields. Added a `remote_row()` builder used by every remote fixture and by `FakeFlatpakTarget` (which now models the filter column, `--filter=` writes, and `--app` filtering of runtimes). The parser still accepts 2–4 fields, since a trailing empty column is omitted.

**Gate**: ruff, basedpyright, codespell, `pytest tests/unit tests/contract` — all green (1583 passed). `tests/unit/test_mutates_audit.py` needed one read-only entry for `_target_refs_now`; that is the only file outside my ownership I touched.

**Not closed**: DIV-11's open point stands — whether flatpak refuses an install its own filter excludes is still unmeasured. Stated in the code comment, the spec doc and the criteria's gap register as the one flatpak survivor.

**Two things for you**: `tests/integration/jobs/test_package_sync.py:1702` still cites `_diff_flatpak_remotes` in prose (out of my file ownership, docstring only). And a race: U4's commit `27c52aad` landed a second before mine and swept up my working-tree edits to the four shared documents — the content is correct and present in HEAD, just attributed to the neighbouring commit.</result>
<usage><subagent_tokens>297445</subagent_tokens><tool_uses>145</tool_uses><duration_ms>1334456</duration_ms></usage>
</task-notification>