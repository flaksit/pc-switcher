I need to validate the new functionality of this PR. This is critical and risky functionality. There are a lot of scenarios that could/should be tested. I want to know what is already covered by pytests. List all possible scenarios/cases (based on "branches" in the requirements logic; not necessarily code paths) and which pytest covers it (or none).

Example of a complicated scenario (not well structured; I hope you structure the scenario's better; maybe it can be broken up into multiple independent sub-scenarios; all well; just to illustrate what I mean by "scenario"):
- Machines A and B
- Package Q was installed with apt on A. On sync A>B, user selected skip-always. So Q is in the machine-specific list of A.
- We are on B. User installs package P. Package P depends on Q. Was not installed on B before. So apt installs automatically as dependency.
- Sync B>A installs package P on A and does nothing with package Q (leaves it as machine-specific).
- (Work on A)
- Sync A>B does nothing: leaves package Q on B even though it is machine-specific to A, because it is a dependence of package P that B has and is kept on B.
- We are on B. User uninstalls package P. The dependent package Q is auto-removed from B.
- Sync B>A removes package P from A. I expect package Q to be automatically kept on A by apt, because it was in apt's list of user-installed packages.
  But even if apt would not have it in its list of user-installed packages, sync B>A keeps it installed on A because it is in A's list of machine-specific packages.

Simpler example
- Machine A: User installed package P from a new apt source. So user added the apt source and the key and installed the package.
- Sync A>B: In review, user chooses "install" for P. Sync adds source, key and installs P.
- On B,
  - User uninstall package P
  - User removes source and key
- Sync B>A: In review, user chooses "delete" for P. Sync uninstalls package P from A and asks the user if sync should remove source and key as well because P was the only package from that source.

Simpler example variant
- Machine B: User installed package R from a new apt source S. So user added the apt source and the key and installed the package.
- Sync B>A: In review, user chooses "skip-always" for R. Sync adds it to the machine-specific list of B.
- Machine A: User installed package P from that same apt source S (which is not yet registered on A). So user adds S and the key and installs the package.
- Sync A>B: In review, user chooses "install" for P. Sync sees source is already there and installs P.
- (Work on B)
- Sync B>A
- On A,
  - User uninstalls package P
  - User removes source and key
- Sync A>B: In review, user chooses "delete" for P. Sync uninstalls package P from B. It leaves source S and the key as is, because needed for machine-specific package R on B.

You see I want to have covered EVERY possible path in the logic of the 4 package sync jobs.

---

I want
- the bugs you found to be challenged and fixed if needed
- tests to be implemented for the gaps for which an automated test is possible

Best you do that now with your already 30% full context (delegating to sub-agents of-course), or handover to two new sessions (1 for the bugs and 1 for the test-gaps)?

---

<task-notification>
<task-id>a24bf0a05e2622b93</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/dd42e8d2-bab5-45db-8c92-4a34a7aab7d4/tasks/a24bf0a05e2622b93.output</output-file>
<status>completed</status>
<summary>Agent "apt_sync coverage-gap tests" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Done. One atomic commit `c884b17a`, `tests/unit/jobs/test_apt_sync.py` only.

## What was added — 12 tests, 6 new classes

| Matrix ID | Test |
| --- | --- |
| B5 | `TestHoldReviewVerbs::test_hold_items_get_their_own_group_with_hold_and_unhold_verbs`, `::test_unhold_group_is_removal_direction_and_the_hold_group_is_not` |
| B6 | `TestInstallBeforeHoldOrdering::test_hold_follows_install_on_the_plain_plan_sort_path`, `::test_hold_follows_install_on_the_accept_review_reorder_path` |
| B7 | `TestHoldOnAnAbsentPackage::test_failed_apt_mark_hold_fails_only_that_item` |
| B10 | `TestHoldsDriveNoSimulation::test_hold_only_run_issues_zero_apt_get_simulations` |
| C24 | `TestRepoGroupRemovalAndKeyChange::test_source_and_its_key_both_removed_with_one_update_after_both` |
| C8 | `TestRepoGroupRemovalAndKeyChange::test_changed_per_repo_key_is_staged_then_promoted_with_the_source_bytes` |
| A10 | `TestManifestIsShowmanualOnly::test_auto_installed_dependency_produces_no_diff_of_any_kind` |
| A12 | `TestManifestIsShowmanualOnly::test_empty_source_manifest_offers_every_target_package_as_an_unticked_removal` |
| D18 | `TestRepoGroupTransaction::test_post_rollback_install_issues_no_further_apt_get_update` |
| D19 | `TestReportOnlyRepoItemDecidedApply::test_apply_on_a_report_only_source_writes_nothing_and_refreshes_nothing` |

## Findings

**No xfails — all ten requirements are met by the implementation.** Including the three you flagged as likely defects:

- **C24** — key and source both removed emit two distinct `sudo rm -f`, and the single `sudo apt-get update` runs after both. (The matrix's separate worry — a *failed* source removal leaving a dangling-key repo when the update runs — is a different scenario and was not in scope.)
- **D18** — `_rollback_repo_group` (`src/pcswitcher/jobs/apt_sync.py:1459`) sets `_metadata_refreshed = True` on a successful re-probe, so the post-rollback install path is a genuine no-op. Exactly two `apt-get update` for the whole run (failing group refresh + re-probe), none after the install.
- **D19** — real end-to-end behaviour: `accept_review`'s `approved_group` test keys on the *decision*, not the action, so the marker **is** inserted and decided APPLY; `apply()` then excludes the REPORT_ONLY source diff, `_approved_repo_group_diffs` filters to INSTALL/REMOVE/CHANGE so `group_diffs` is empty, and `_ensure_repo_group_converged` short-circuits to `{marker: (True, "no repository changes to refresh for")}` before ever resolving `$HOME`. Net: marker inserted, converges as a no-op, zero `/etc/apt` writes, zero `apt-get update`, no `send_file`, no exception. The test pins all of that.

## Notes

- B6 covers both branches as asked. The "plain `plan()` sort" branch needed an *unapproved* repo diff present so `plan()` actually runs its `_ITEM_CLASS_ORDER` sort while `accept_review` skips the marker rebuild — otherwise `plan()` returns `base_plan` unsorted and the branch isn't exercised.
- D19 reads `job._accepted_plan` / `job._accepted_outcome` to assert marker insertion; there is no black-box observable for it (the marker converges silently). `pyrightconfig.json` sets `reportPrivateUsage: false` for `tests`, so this is clean.
- New imports: `pathlib.Path`, `apt_sync._METADATA_REFRESH_ITEM_ID`, `review._REMOVAL_ACTIONS` (the last is the actual predicate the reviewer uses for unticked-by-default, per your instruction).

## Verification

`ruff check` / `ruff format --check` / `basedpyright` all clean on the file (0 errors). `pytest tests/unit/jobs/test_apt_sync.py` → **85 passed**. Full `pytest tests/unit` → **1041 passed**.

One caution: I initially ran `uv run ruff check --fix .` and `ruff format .` repo-wide before scoping to my file. `ruff format` reported "1 file reformatted" (my file was the only one I had edited, and it is the one that needed formatting), and the check surfaced — but did not fix — three `F821 Undefined name 'yaml'` errors in `tests/unit/jobs/test_package_state.py`, another agent's file mid-edit. Those are gone now and the full suite is green, so nothing appears to have been clobbered, but flagging it since concurrent agents were in flight.</result>
<usage><subagent_tokens>197492</subagent_tokens><tool_uses>30</tool_uses><duration_ms>411063</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a269b7dc9bcf9f9ae</task-id>
<tool-use-id>toolu_019S8exmPFW31b1A4XhtbD4x</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/dd42e8d2-bab5-45db-8c92-4a34a7aab7d4/tasks/a269b7dc9bcf9f9ae.output</output-file>
<status>completed</status>
<summary>Agent "Fix hold skip-always leak" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Done. Two commits: `3cf963be` (src fix) and `39112558` (tests).

## What changed

`src/pcswitcher/jobs/packages/sync_core.py`
- New `PackageSyncJob._decision_holder_is_source(action)` — the single D-08a holder rule (INSTALL/CHANGE → source, REMOVE → target). `_record_permanent_skips` now calls it instead of inlining the tuple check, so write path and read path share one definition.
- New `PackageSyncJob._drop_inert_diffs(diffs, source_decisions, target_decisions)` — drops any `ItemDiff` whose `item_id` is recorded on the machine that holds it; `REPORT_ONLY` diffs pass through (never recorded, so no holder).
- Base `plan()` now stores the loaded pair on `self._plan_decisions` (re-assigned per call, not cached across calls) and pipes `diff_items(...)` through the new filter.
- Module docstring's false claim ("made real at the diff-input boundary rather than as a later filter on the review") replaced with what the code does and why: membership items derive their identity from set membership over package state, so their id first exists on the `ItemDiff`; for apt the post-diff pass is also the only correct place.

`snap_sync.py`, `flatpak_sync.py`, `apt_sync.py` — filter applied at the tail of each `plan()`. apt reuses `self._plan_decisions` from `super().plan()` rather than re-reading (so its repo/collateral diffs are covered too — repo diffs had the same gap, being digest-derived); flatpak's call is a documented no-op backstop keeping all four tails identical.

## Proof

`tests/unit/jobs/test_block_state_decisions.py` (10 tests, new file only). Each case runs two rounds: round 1 plans, decides SKIP_ALWAYS, applies; the written YAML is extracted from the real `printf '%s' &lt;quoted content&gt;` record command and replayed as round 2's decision-file `cat` — so round 2 consumes exactly the bytes round 1 wrote, not a hand-built copy. Round 2 asserts absence from both `plan.diffs` and every review group's entries.

Covered: apt hold INSTALL (recorded on source, target file untouched) and unhold REMOVE (recorded on target, source untouched); same both directions for snap; flatpak mask/unmask positive control. Plus: a decision planted on the wrong machine does **not** silence the diff; a recorded snap hold does not silence the snap's own `snap:alpha` presence diff.

Verified non-vacuous — with `src/` stashed, 7 of the 10 fail (all apt + snap cases) and the 3 flatpak controls pass, confirming flatpak was already correct and stays correct.

## The apt suppression trap held

`TestAptHeldPackageSuppression`: pkg-b is 2.0 on the source, 1.0 and held on the target. With `apt:hold:pkg-b` itself recorded on the target, plan emits neither the hold diff nor `apt:package:pkg-b` — the upgrade stays suppressed because `target_hold_names` is still unfiltered. Second test: with an unrelated `apt:hold:pkg-c` recorded, pkg-c's diff is gone while `apt:hold:pkg-b` is still offered and `apt:package:pkg-b` is still suppressed.

## Gate status

`ruff check`, `basedpyright` and `pytest tests/unit` (1060 passed, 1 xfailed) are clean for my files. Two pre-existing failures belong to other agents' in-flight work and I left them alone: ruff F821/F401 in `tests/unit/jobs/test_manual_installs_sync.py` and `tests/unit/test_step_gate.py`, and a basedpyright `reportUnusedFunction` on `_is_actionable_group` in `src/pcswitcher/jobs/packages/review.py` (that file is modified in the working tree by another agent; not staged or committed by me).

Note: one `uv run ruff format .` ran repo-wide before I narrowed scope, so it may have reformatted (not semantically changed) other agents' uncommitted test files.</result>
<usage><subagent_tokens>124529</subagent_tokens><tool_uses>37</tool_uses><duration_ms>380239</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a2bb24ab9af01520e</task-id>
<tool-use-id>toolu_01Whyb4htzbSa88tbATNAkvh</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/dd42e8d2-bab5-45db-8c92-4a34a7aab7d4/tasks/a2bb24ab9af01520e.output</output-file>
<status>completed</status>
<summary>Agent "CLI gate + mutates audit tests" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Done. Commit `43a66586`, three files, all green (`ruff format`/`check`, `basedpyright` 0 errors).

## Tests added

**M12** — `tests/unit/cli/test_commands.py::TestConfirmEachCommandFlag` (3 tests): the flag is refused when `is_interactive` is false — exit code 1, error text contains `--confirm-each-command`, and neither `_load_configuration` nor `_run_sync` is called (refused before the run starts); on a TTY it is forwarded as `confirm_each_command=True`; a non-interactive run *without* the flag is not refused (scopes the refusal).

**M13** — new file `tests/unit/test_mutates_audit.py` (3 tests). AST walk of every `src/pcswitcher/**/*.py`, matching `run_command`/`start_process`/`send_file`/`get_file` by method name only (no receiver filter, so renaming a handle cannot dodge it). Read-vs-write is never inferred from the command string: each ungated call site is accounted for by key `&lt;relpath&gt;::&lt;enclosing qualname&gt;::&lt;method&gt;` (line-number-free, so unrelated edits don't churn it) in either `_READ_ONLY_CALLS` (count per function) or `_UNGATED_WRITES` (one entry per write, with what it changes and the issue tracking it). Failure message prints file:line and the unparsed call for every offender. Also fails on stale entries, and a guard test asserts the walk still finds call sites at all. Documented blind spots: calls reached via a callable passed by reference (`_capture_dir_digests(source_run, …)` — reads only today) and `declare_modification` (where `mutates` is required).

**M14** — `tests/unit/test_step_gate.py::TestStateWritesReachTheGate` (8 tests): `DecisionFile.record` and `SnippetRegistry.add` each trigger exactly one `confirm_action`, parametrized over both roles using the **real** `LocalExecutor` (with `create_subprocess_shell` patched) and the real `RemoteExecutor`; host is SOURCE/TARGET respectively, description names the item, command names the file. Plus: abort stops the write, the registry `send_file` push is gated on the target, and `load()` on either store never prompts.

## xfail (strict) — report this

`test_every_ungated_write_is_tracked` is `xfail(strict=True)`. **16 modifications reach a machine without `mutates=`**; 1 is tracked (#209), **12 entries covering 15 writes are untracked**:

| file:line | what it changes |
| --- | --- |
| `src/pcswitcher/btrfs_snapshots.py:81` | creates a read-only btrfs snapshot subvolume |
| `src/pcswitcher/btrfs_snapshots.py:105` | creates `/.snapshots` subvolume + `/.snapshots/pc-switcher` |
| `src/pcswitcher/btrfs_snapshots.py:266` | `btrfs subvolume delete` of a snapshot |
| `src/pcswitcher/btrfs_snapshots.py:278` | `rmdir` of the emptied session folder |
| `src/pcswitcher/btrfs_snapshots.py:338` | deletes EVERY pc-switcher snapshot on the machine |
| `src/pcswitcher/config_sync.py:349` | creates `~/.config/pc-switcher` on the target |
| `src/pcswitcher/config_sync.py:363` | `send_file` overwriting the target's `config.yaml` |
| `src/pcswitcher/jobs/btrfs.py:151` / `:186` | creates the session snapshot folder on source / target |
| `src/pcswitcher/jobs/install_on_target.py:65` | runs the installer script on the target (installs/upgrades pc-switcher) |
| `src/pcswitcher/jobs/vscode_state_sync.py:364` | creates the target's editor state dir |
| `src/pcswitcher/jobs/vscode_state_sync.py:369` | `send_file` of the merged `state.vscdb` |
| `src/pcswitcher/jobs/vscode_state_sync.py:373` | target-inject SQL against the staged DB |
| `src/pcswitcher/jobs/vscode_state_sync.py:381` | `mv -f` over the user's live `state.vscdb` |
| `src/pcswitcher/lock.py:138` | creates `~/.local/share/pc-switcher` and writes the lock holder file on the target |

Tracked exemption: `src/pcswitcher/jobs/folder_sync.py:826` — the rsync mirror pass (`#209`), listed in `_UNGATED_WRITES` with `tracked_by="#209"` so it does not fail the audit.

Judged read-only and allowlisted rather than flagged: `apt-get -s` (simulation), `lock.py:152`'s `flock -n … -c read` (advisory lock on a file already written), and the `dummy_success`/`dummy_fail` demo `seq`/`echo`/`sleep` loops.

## Note

`uv run pytest tests/unit -q` reports **4 pre-existing failures in `tests/unit/jobs/test_package_review.py`** (`TestInteractive::…`) from another agent's in-flight work on `src/pcswitcher/jobs/packages/review.py`. Not caused by and not touched by this commit; my three files are 39 passed, 1 xfailed.</result>
<usage><subagent_tokens>133209</subagent_tokens><tool_uses>30</tool_uses><duration_ms>487986</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ab4026085c391ead9</task-id>
<tool-use-id>toolu_01LBCHGmZ2sFUoFQdsVgFhgG</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/dd42e8d2-bab5-45db-8c92-4a34a7aab7d4/tasks/ab4026085c391ead9.output</output-file>
<status>completed</status>
<summary>Agent "Gate the 15 ungated writes" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Done. All 15 writes are gated; audit is green without `xfail`.

## Phrases

`btrfs_snapshots.py`
- `create_snapshot` — `create read-only snapshot {snapshot_path} of {source_path}`
- `validate_snapshots_directory` — `create the /.snapshots subvolume and its pc-switcher folder on this machine`
- `cleanup_snapshots` delete — `permanently delete snapshot {snap.path} and everything it preserves`
- `cleanup_snapshots` rmdir — `remove the emptied snapshot session folder {folder_path}`
- `delete_all_snapshots` — `permanently delete EVERY pc-switcher snapshot on this machine, losing all rollback points`

`config_sync.py`
- mkdir — `create the pc-switcher config directory on the target`
- send_file — `overwrite the target's pc-switcher config at {CONFIG_REMOTE_PATH} with this machine's copy`

`jobs/btrfs.py` (source and target, identical text; the executor supplies the host)
- `create the snapshot session folder /.snapshots/pc-switcher/{session_folder}`

`jobs/install_on_target.py`
- `install pc-switcher {label} on the target, replacing any existing installation` — `label` derived from the `Release | Version | str | None` argument (`v.tag`, `v{semver}`, branch name, or `main`), so the prompt names what will land.

`jobs/vscode_state_sync.py` (`label` = `db_label(relpath)`, e.g. `Code state.vscdb`)
- mkdir — `create the editor state directory for {label} on the target`
- send_file — `upload this machine's {label} to a staging file beside the live one`
- target-inject — `write the target's own preserved {label} keys into the staged copy`
- `mv -f` — `replace the target's live {label} with the merged database, discarding the current file`

`lock.py`
- `claim the target's sync lock file, overwriting any previous holder record`

No read gained `mutates=`; the read-only entries (`echo $HOME` in config_sync, the `--version` probes, `test -f`, the `flock` start_process) were already in `_READ_ONLY_CALLS` and their counts still match, so `test_no_ungated_call_site_is_unaccounted_for` passes unchanged.

## Audit

`_UNGATED_WRITES` now holds only the `#209` folder_sync rsync entry; the `@pytest.mark.xfail` is removed (and the then-unused `pytest` import with it). Audit logic untouched — the `&gt;40 ungated call sites` sanity check still passes. Docstring reworded for the new state.

## Tests adapted

- `tests/unit/cli/test_config_sync.py` — two `send_file.assert_called_once_with(...)` now include `mutates=ANY` (added `ANY` import).
- `tests/unit/jobs/test_snapshot_job.py` — `run_command_not_subvolume` and `run_command_space_error` side-effect stubs take `**_kwargs: object`.

## Docs

`docs/system/core.md` coverage paragraph rewritten (was "every write the four package sync jobs make"), and its "folder_sync has not yet been marked up" sentence now points at the audit test. `src/pcswitcher/step_gate.py` module docstring's "today only the package sync jobs pass `mutates`" corrected.

## Verify

`uv run ruff format . &amp;&amp; uv run ruff check . &amp;&amp; uv run basedpyright &amp;&amp; uv run pytest tests/unit -q` → clean; 0 errors/warnings; 1097 passed.

Committed as `61cfe69 fix(executor): gate the last 15 unannounced writes behind mutates=` with the repo's `Co-Authored-By` / `Claude-Session` trailers. (I did not fabricate the `Entire-Checkpoint` trailer the recent commits carry — it looks harness-generated.) The untracked `.planning/phases/02-package-management-sync/02-SCENARIO-COVERAGE.md` was left alone.</result>
<usage><subagent_tokens>94236</subagent_tokens><tool_uses>57</tool_uses><duration_ms>300856</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aaaab56e3e3900272</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-janfr-dev-pc-switcher/dd42e8d2-bab5-45db-8c92-4a34a7aab7d4/tasks/aaaab56e3e3900272.output</output-file>
<status>completed</status>
<summary>Agent "Update coverage matrix" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Committed as `48ebf2ab`. Gate is clean (ruff format/check, basedpyright 0 errors, 1097 unit tests pass).

## Rows that changed coverage state: 34

Promoted to `U` (32): A10, A12, B5, B6, B7, B9, B10, C8, D18, D19, D20, E14, E15, E16, E18, E22, F15, F16, F17, F20, G22, G24, G30, H6, H7, H18, I6, I15, M12, M13, M14, plus J10 gaining a unit half.

Reclassified without gaining a test (2): F21 (`—` → `‼`, behaviour now pinned by `TestRemoteRemovalOrphansRefs` but no guard exists), K2 (`U` → `P`, see below).

Three rows added: C27 (skip-always on digest-derived apt repo items), H19 (REPORT_ONLY deliberately not offered skip-always, per the ADR-020 2026-07-25 amendment), I23 (abort/teardown/markup at the promotion checkbox). A8, I5, N3, N8 gained citations. Legend: `‼` redefined as "open defect, or a requirement that is not implemented"; `V` now explicitly states it is a claim about the test, not a passing run.

## Citation that did not hold up

K2 — "apt: no passwordless sudo on target → error naming the binaries", cited as `apt (target sudo path in TestValidate)`. `AptSyncJob.validate` (apt_sync.py:1672) does run the target check and builds a hint from `_TARGET_SUDO_COMMANDS`, but `TestValidate` (test_apt_sync.py:1435) has exactly four tests — all-pass, apt-mark missing, dpkg lock, and source sudo. Nothing drives a target sudo failure. Downgraded to `P`. This was the only vague citation in the matrix; the other 309 backticked test/class names all resolve to real collected tests, and I spot-checked the bodies of the snap/flatpak/manual `validate` citations, which do hold.

## Still uncovered after re-derivation

Open (no implementation): E17 sideloaded `x&lt;N&gt;` revisions (deferred, needs the diff-class product call), F21 remote orphaning, C26/N7 repo removal vs machine-specific package, N6 last-package-from-this-source prompt.

Gaps in behaviour believed correct: K2 (above), C27 (the apt `plan()` inertness pass is only ever tested with a hold item, never a `apt:source:`/`apt:key:` one — this is the latent half of the `3cf963be` fix), A11, C5, K7, L11, N8 round trip, I22/N11 (UAT-only by nature).

Unverified pending CI: every `V` row. `integration-tests.yml` triggers only on PRs targeting `main`, so the stacked PR skips the VM suite. L10's #208 D9 verdict stays UNDETERMINED — `TestSnapHoldCaptureTiming` encodes the different-namespaces premise the capture ordering rests on, and real snapd has not answered.</result>
<usage><subagent_tokens>131538</subagent_tokens><tool_uses>64</tool_uses><duration_ms>512409</duration_ms></usage>
</task-notification>